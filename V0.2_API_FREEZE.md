# buTUI v0.2 API Freeze

> 状态：v0.2.0-frontier API freeze candidate  
> 基线：`bc77e59 feat(stream): freeze retention defaults and sidecar lifecycle`  
> 配套：[STABILITY.md](./STABILITY.md) / [V0.2_CONTRACTS.md](./V0.2_CONTRACTS.md)

## 0. 冻结结论

v0.2 是 **opt-in runtime upgrade**：

- v0.1 入口和默认行为不破坏；
- v0.2 新能力先标为 **experimental**；
- stable 层只接受兼容性修复；
- experimental 层可以在有 migration note 的前提下演进；
- 不新增 native core、WASM backend、远程协议或新的存储后端。

## 1. Stable：v0.1 兼容面

以下入口继续按 `STABILITY.md` 的兼容规则维护：

```text
@butui/runtime     createTuiApp / TuiApp 的 v0.1 方法
@butui/core        Node / event / focus / theme / revision
@butui/layout      Cell / Line / Frame / layout / selectionText
@butui/renderer    Renderer / paintLine / plainText
@butui/terminal    TerminalSession / InputDecoder / OSC 22 / OSC 52
@butui/stream      StreamSource / MarkdownStream / DiffStream
@butui/components  已冻结的 v0.1 组件
@butui/keymap      CommandRegistry / Keymap
@butui/plugins     Plugin / SlotRegistry
@butui/agent       AgentEvent / UiCommand / Session
@butui/undo        Journal / patch / plan
@butui/image       createImage / ImageLayer / renderImage
```

稳定面不得依赖 experimental 类型作为必需参数；新增能力必须可通过可选字段接入。

## 2. Experimental：v0.2 新 API

| 包 | Experimental API | 语义 |
|---|---|---|
| `@butui/core` | `FrameClock` / `AdaptiveQuality` | 单时钟、lane、预算、质量降级 |
| `@butui/core` | `MemoryLedger` / `DEFAULT_MEMORY_POLICY` | allocation admission / spill candidates |
| `@butui/runtime` | `TuiPaintStats` | `frameId` / `accepted` / damage 统计 |
| `@butui/runtime` | `waitUntilFrameFlushed()` | accepted / drained 屏障 |
| `@butui/runtime` | `inputRouting: "presented"` | 鼠标按用户实际看到的 frame 路由 |
| `@butui/renderer` | `computeDamage()` / `FrameDamage` | full / line span damage |
| `@butui/terminal` | `TerminalArbiter` | 单写者 lease / supersede / backpressure |
| `@butui/terminal` | `TerminalModeJournal` | mode 逆序关闭 / 正序恢复 |
| `@butui/terminal` | `noteExternalWrite()` | best-effort external stdout full damage |
| `@butui/stream` | `StreamLedger` / `StreamEnvelope` | revision / gap / replace-tail / cancel |
| `@butui/stream` | `FileSpillStore` / `SpillStore` | cold storage / reopen / compact |
| `@butui/stream` | `DEFAULT_RETENTION_POLICY` | 冻结的默认 hot / sidecar 预算 |
| `@butui/stream` | `createStreamWindow()` / `<StreamWindow>` | stable + volatile viewport |
| `@butui/components` | `StreamWindow` | ledger-backed transcript 窗口 |
| `@butui/plugins` | Worker / process isolation APIs | 实验性隔离与 capability 层 |

Experimental API 可以有 breaking change，但必须满足：

1. 更新本文件和对应报告；
2. 给出 v0.1 → v0.2 迁移路径；
3. 保留至少一个兼容周期，或提供 adapter；
4. 不要求应用安装 native toolchain。

## 3. 默认行为冻结

| 选项 | v0.2 默认 | 说明 |
|---|---|---|
| `render.mode` | `"microtask"` | 保持 v0.1 |
| `render.fps` | `120` | 仅 frame mode |
| `render.adaptiveQuality` | `false` | 显式 opt-in |
| `inputRouting` | `"logical"` | 保持 v0.1；presented 显式开启 |
| `mouseMotion` | `"drag"` | hover 显式开启 |
| `mousePointer` | `true` | 可关闭 |
| `waitUntilFrameFlushed` | `"accepted"` | drained 显式请求 |
| `StreamWindow.follow` | `false` | live transcript 显式开启 |
| `cleanupSidecars` | `false` | 默认保留供恢复 |
| retention | `4MiB / 256 lines` | 见下 |
| applied/index cache | `64 chunks` | sidecar 开启时 |
| compactAfterDeletes | `10_000` | 0 表示关闭 |

冻结 retention：

```ts
{
  hotBytes: 4 * 1024 * 1024,
  keepTailLines: 256,
  appliedCacheChunks: 64,
  indexCacheChunks: 64,
  compactAfterDeletes: 10_000,
}
```

## 4. API surface snapshot

运行时 value exports 快照：

```text
api-surface.json
scripts/api-surface.ts
tests/api-surface.test.ts
```

检查：

```bash
bun --conditions=browser run scripts/api-surface.ts --check
```

只有在 API review 后才允许刷新：

```bash
bun --conditions=browser run scripts/api-surface.ts --write
```

## 5. v0.1 → v0.2 迁移

应用不需要改动即可继续使用 v0.1 默认行为。需要 v0.2 能力时按需启用：

```ts
const app = createTuiApp({
  render: { mode: "frame", fps: 120, adaptiveQuality: true },
  inputRouting: "presented",
  view: () => <StreamWindow ledger={ledger} streamId="transcript" follow revision={revision} />,
});

await app.waitUntilFrameFlushed(undefined, "drained");
```

迁移顺序建议：

1. 先启用 `frame` + `waitUntilFrameFlushed`；
2. 再启用 `inputRouting: "presented"`；
3. 最后接入 StreamLedger / StreamWindow / retention；
4. terminal raw / PTY 场景再启用 `TerminalArbiter` / mode journal。

## 6. 不进入 v0.2 冻结面

- WASM / Rust accelerator；
- native renderer / render thread；
- 多终端 surface / remote collaboration；
- 水平 ScrollBar；
- 复杂 stagger 编排；
- 主题 / 组件目录扩张；
- bugent 协议进入 core。
