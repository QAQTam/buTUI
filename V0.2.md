# buTUI v0.2.0 设计草案

> 分支：`design/v0.2.0`  
> 状态：架构决策草案，不进入实现  
> 前置：v0.1 已冻结，bugent 第一版接入清单见 `BUGENT_V1.md`

## 0. 结论

v0.2.0 不应该做三件事：

1. 不把 Rust 重写成整个 runtime。
2. 不追求 OpenTUI feature parity。
3. 不为了“更像原生”而提前引入 native core。

v0.2.0 应该做四件事：

1. **继续吸收 OpenTUI 的接口与组件边界**，尤其是 Renderable、焦点、输入、
   组件组合和终端能力语义。
2. **建立语言无关的帧内核**：统一 FrameClock、Damage / DisplayList、
   backpressure 和自适应质量。
3. **把 Rust 作为可选热路径加速器**，通过 WASM / native accelerator 接口接入，
   而不是重写宿主。
4. **先服务 bugent 真实工作负载**，用 PTY 下的数据决定优化顺序。

一句话：**接口继续借鉴 OpenTUI，架构走自己的“可移植帧内核 + 可选原生加速”
路线。**

## 1. 为什么不是现在就 Rust 重写

Rust 适合解决的问题：

- 超大规模 cell / text 的解析、测量、折行、diff。
- 需要稳定内存布局和可控分配的内核。
- 多线程 / worker 中的 CPU 密集计算。
- 跨语言共享紧凑 buffer，而不是频繁跨边界传对象。

Rust 不适合当前阶段解决的问题：

- Solid / Bun 的响应式生命周期。
- 组件组合、focus、keymap、应用状态。
- bugent 事件协议与 UI 状态同步。
- 终端能力探测、OSC、输入解码这类 I/O 边界。

Rust 重写的真实成本：

- Bun ↔ Rust 的 ABI、异常、异步和生命周期桥接。
- macOS / Linux / Windows 构建矩阵和发布体积。
- native crash 的调试与回滚。
- 两端类型模型重复，边界复制可能抵消收益。
- 开发者需要同时维护 TS 语义和 Rust 语义。

v0.2 先用基准证明“瓶颈在内核”，再决定是否 native。当前已有证据：

- 流式文本 push / layout / paint 已保持 O(delta + W)。
- smooth reveal 120fps 的主要成本是 tick 数量，不是语言吞吐。
- 慢终端风险已通过 stdout backpressure 收敛。
- bugent 尚未跑真实 PTY，因此还没有可信的瓶颈分布。

## 2. OpenTUI 借鉴边界

### 继续借鉴

- Renderable / component catalogue 的职责划分。
- 焦点、键盘、鼠标、滚动、弹窗和表单的交互语义。
- 组件 API 的可组合性，不强制应用使用某一种状态管理。
- 终端能力探测和降级策略。
- 性能文档中已经验证过的设计取舍。

### 不照搬

- Zig / C FFI 作为唯一核心路径。
- native renderer 与 JS 组件层的强绑定。
- 为了 feature parity 引入大量低频组件。
- 用 native 依赖换取尚未被真实工作负载证明的收益。
- 把布局、响应式、终端 I/O 全部压进一个 native runtime。

## 3. v0.2 目标架构

```text
bugent / app
  │
  ├─ protocol / commands
  │
  ▼
@butui/runtime
  ├─ FrameClock（单一时间源 + 优先级）
  ├─ Damage / DisplayList（可测试、可序列化）
  ├─ AdaptiveQuality（120fps 目标，按预算降级）
  ├─ BackpressureGate
  └─ Plugin / Worker boundary
       │
       ▼
@butui/core + @butui/layout
  ├─ scene graph / incremental layout
  ├─ typed cell / line buffers
  └─ optional accelerator ABI
       │
       ├─ TS kernel（默认，零 native）
       ├─ WASM kernel（跨平台优先）
       └─ Rust native kernel（实测后再开）
```

### 3.1 单一 FrameClock

现在存在多个时间源：

- runtime frame scheduler
- AnimationScheduler
- smooth reveal scheduler
- 各组件自己的 timer / interval

v0.2 应统一成一个 clock，并把工作分成优先级：

```text
input / focus      最高优先级，目标 < 1 frame
state / layout     下一帧必须处理
stream reveal      120fps 目标，可合并
decorative anim    可降级、可暂停
telemetry          低优先级
```

目标不是“所有东西都 120fps”，而是：

- 输入延迟不被动画拖累。
- 120fps 是目标值，不是硬保证。
- 超预算时先降低装饰动画，再合并 reveal，最后才牺牲输入响应。

### 3.2 Damage / DisplayList

把 layout 输出从“完整 frame 对象”扩展成可描述变化的协议：

```ts
interface Damage {
  full: boolean;
  lines?: Array<{ y: number; from: number; to: number }>;
  graphics?: Array<{ id: string; rect: Rect }>;
}
```

第一版仍由 TS renderer 消费，后续 Rust / WASM 可以消费同一个紧凑 buffer。

要求：

- damage 与 renderer 状态机解耦。
- 支持 line / span / cell / graphic 四种粒度。
- damage 计算可跳过：backpressure 时只更新内存，不生成无用 ANSI。
- 所有结果可在 headless 测试中复现，不依赖终端。

### 3.3 自适应质量

v0.2 引入明确的质量档：

| 档位 | 目标 | 行为 |
|---|---|---|
| `full` | 120fps | smooth reveal、shimmer、动画全开 |
| `balanced` | 60fps | 合并装饰动画，保留流式 reveal |
| `responsive` | 输入优先 | 暂停装饰动画，reveal 加速追赶 |
| `minimal` | 兼容优先 | 关闭动画，只保证最终内容与输入 |

降级依据：

- 实际 frame duration 的 p95。
- stdout backpressure 持续时间。
- event loop delay。
- 连续 skipped tick。
- 用户显式 reduced-motion。

不允许自动关闭：

- 输入处理。
- 最终内容一致性。
- 终端恢复。
- 焦点 / 鼠标 / 剪贴板的基础语义。

### 3.4 Rust / WASM 加速器边界

v0.2 只定义 accelerator ABI，不默认启用 native。

候选内核，按收益排序：

1. ANSI parse / SGR state / cell conversion。
2. grapheme + width + wrap。
3. line / span diff。
4. layout measure（收益不确定，先不迁）。
5. terminal input decode（I/O 占比高，通常不值得）。

接入顺序：

```text
TS baseline
  → WASM spike（验证 ABI 和收益）
  → Rust native（只有 WASM/TS 不达标才开）
```

必须满足的门槛：

- 该内核占 p95 frame budget 的 20% 以上。
- WASM / Rust 相比 TS 至少 2×，或显著降低内存波动。
- 失败时可无缝回退 TS。
- 不改变 public API。
- 不要求 bugent 安装 native 工具链。

## 4. v0.2 里程碑

### M0：真实负载与可观测性

- 在 bugent 接真实 PTY 跑 2000 chunk/s。
- 采集 frame time、input latency、stdout backpressure、heap、GC、skipped ticks。
- 建立 10k / 100k / 1M 行转录的内存曲线。
- 输出第一份 profile，不先改架构。

### M1：统一 FrameClock

- runtime / animation / smooth / inertia 共用 clock。
- 定义优先级和 frame budget。
- 输入优先，装饰动画可降级。
- 测试 fake clock 下确定性。

### M2：Damage / AdaptiveQuality

- layout 输出 damage。
- renderer 消费 damage，不再每次扫描完整帧。
- 加入 full / balanced / responsive / minimal。
- 慢终端和 CPU 饱和时自动降级。

### M3：Accelerator spike

- 定义 C ABI / WASM 数据布局。
- 先做 ANSI + wrap 的 WASM 原型。
- 与 TS baseline 对比；不达标就停止，不引入 Rust 依赖。
- 只有通过门槛才开 Rust native。

### M4：插件隔离与 bugent 硬化

- Worker / process 隔离不可信插件。
- 权限审批与 capability 记录。
- bugent 的事件重放、错误隔离、退出恢复。

## 5. 验收指标

v0.2 不按“组件数量”验收，按以下指标：

- 输入到可见反馈的 p95 延迟。
- frame duration p50 / p95 / p99。
- 120fps 下连续 10 分钟的 heap 增长曲线。
- 2000 chunk/s 下 stdout backpressure 次数和最大缓冲。
- 100k 行转录的 push / layout / paint 成本是否仍为 O(delta + viewport)。
- 降级切换是否无闪烁、无内容丢失。
- TS / WASM / Rust backend 结果是否逐 cell 一致。

## 6. 非目标

- 不在 v0.2 重写 Solid renderer。
- 不把 bugent 协议塞进 core。
- 不做 OpenTUI feature parity。
- 不默认发布 native binary。
- 不做 WebUI diff / 自定义 renderable catalogue。
- 不做水平 ScrollBar、复杂 stagger 编排等低优先级功能。

## 7. 待确认

1. bugent 第一版支持哪些平台（Linux / macOS / Windows / WSL）？
2. 可接受的 native 依赖和发布体积是多少？
3. 100k 行转录的 memory budget 是多少？
4. 是否允许 WASM 作为默认加速路径？
5. 插件是否必须跨进程隔离，还是 worker 隔离足够？

这些问题由 bugent 真实第一版的数据回答，不在设计阶段提前猜。
