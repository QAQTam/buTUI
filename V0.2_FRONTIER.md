# buTUI v0.2 Frontier：从帧运行时到可重放终端会话

> 分支：`design/v0.2.0-frontier`  
> 基线：`f20d442`（`design/v0.2.0`）  
> 状态：探索性架构设计，不改变 v0.1 公共契约  
> 接口细节：[V0.2_CONTRACTS.md](./V0.2_CONTRACTS.md)
>
> P0 设计：[V0.2_P0_FRAME_CLOCK.md](./V0.2_P0_FRAME_CLOCK.md) /
> [V0.2_P0_INPUT_ROUTING.md](./V0.2_P0_INPUT_ROUTING.md)

## 0. 一句话

**v0.1 把终端渲染做成了增量帧；v0.2 要把终端 UI 做成一个有因果、有预算、
可重放、可仲裁的长期会话。**

```text
Session 是真相。
Projection 是解释。
Frame 是缓存。
Terminal 是被租用的单写者。
Memory 是准入资源。
Capability 是插件能看到的世界边界。
```

这不是“再抄一层 OpenTUI”。OpenTUI 的 Renderable / backend / native span feed
仍值得借鉴；Ink 的调度和 flush 语义仍值得借鉴。但 buTUI 面对的是另一个问题：

- agent 会话可以运行数小时；
- 输入、工具、权限、reasoning、diff 并发到来；
- 输出可能被慢终端、子进程、console、调试器打断；
- 同一个 session 要在 TUI、SSH、Web 和测试回放中保持一致；
- 插件既要有 UI 权限，也要有独立于 OS 沙箱的能力边界。

因此 v0.2 的第一性对象不应是组件或 cell，而应是 **可重放的 Session 和受控的
Terminal 所有权**。

## 1. 四平面模型

```text
┌──────────────────── Session Plane ────────────────────┐
│ input / agent events / stream ops / capability calls  │
│ append-only log + snapshots + deterministic reducer   │
└──────────────────────────┬────────────────────────────┘
                           │ revision
┌──────────────────── Projection Plane ─────────────────┐
│ semantic scene → retained node tree → layout → Frame  │
│ pure-ish, droppable, reproducible, viewport bounded    │
└──────────────────────────┬────────────────────────────┘
                           │ FrameTicket
┌──────────────────── Transport Plane ──────────────────┐
│ FrameClock → Damage → ANSI → TerminalLease → stdout   │
│ latest-wins, backpressure-aware, single writer         │
└──────────────────────────┬────────────────────────────┘
                           │ budgets / quality / policy
┌──────────────────── Control Plane ────────────────────┐
│ MemoryLedger / AdaptiveQuality / CapabilityBroker      │
│ replay policy / retention / suspension / recovery      │
└───────────────────────────────────────────────────────┘
```

### 1.1 Session Plane

Session 只保存能决定未来的状态：

- 用户输入和命令；
- agent 事件；
- stream op；
- capability 调用和审批；
- 影响输出的时间和随机源；
- 必要的 snapshot。

Frame、ANSI、layout cache、renderer previous-line buffer 都不是 Session 真相，
可以随时丢弃并从 Session 重建。

### 1.2 Projection Plane

现有 `Node` 树继续作为 projection 实现，不要求业务状态迁入一个全局 store。
`layout()` 产出不可变 `Frame`，但 Frame 必须携带：

- session revision；
- projection revision；
- frame id；
- viewport 和终端能力；
- damage 描述；
- 可用于 hit test 的稳定 node / semantic id。

### 1.3 Transport Plane

Transport 不再只是 `renderer.draw(frame)`：

- 只允许一个 writer 拥有终端；
- frame 可以 superseded，但控制序列不能丢；
- 指针命中必须针对用户实际看到的 frame；
- stdout backpressure 期间不排队旧 frame；
- suspend / resume 后强制 full damage。

### 1.4 Control Plane

Control Plane 决定“现在能不能做”：

- 当前质量档；
- 当前内存预算；
- 当前 terminal lease 属于谁；
- 插件 capability 是否有效；
- replay 是否使用真实时间、虚拟时间或最快时间。

它不直接画东西，但可以拒绝一次 allocation、降级一类动画或暂停一个 plugin slot。

## 2. v0.2 的五个原语

### 2.1 Revision，而不是布尔 dirty

v0.1 用 `dirty` 表示“需要重画”。v0.2 要回答“画的是哪个世界”：

```text
input seq → session revision → projection revision → frame id
```

每次用户输入、stream op、resize 和 capability 变化都推进 session revision。
Frame 记录自己来自哪个 revision。事件不能对未知 revision 做隐式猜测。

### 2.2 Transaction，而不是散落 write

一帧是一笔输出事务：

```text
plan → flush state → layout → damage → encode → lease write → commit/abort
```

事务提交前可以失败、重排或被更新 revision 取代。提交后：
- `lastPresentedFrame` 才能更新；
- 鼠标 hit test 才能切到新坐标；
- 旧 frame 的 ANSI 不再允许写终端。

### 2.3 Lease，而不是 stdout 自由写

终端是单写者资源。Frame、console、子进程 PTY、调试器都必须通过
`TerminalArbiter` 获得 lease。lease 支持：

- `frame`：可覆盖、可 supersede；
- `append`：日志和外部文本，必须保持顺序；
- `raw`：暂时把终端交给子进程 / 调试器；
- `suspend/resume`：释放和恢复时自动 invalidate。

无法拦截的直接 `stdout.write()` 只能被检测并触发 full redraw，不能假装已被仲裁。

### 2.4 Ledger，而不是无限 append

所有长期增长的对象都记账：

- transcript lines / bytes；
- stream stable chunks / volatile tail；
- undo / replay journal；
- plugin output；
- frame snapshots。

`MemoryLedger` 不是事后 GC 建议，而是 allocation 前的 admission control。

### 2.5 Capability，而不是“插件自己自觉”

插件获得的是 capability token：

- 能挂载哪些 slot；
- 能读哪些 semantic region 的事件；
- 能调用哪些 command；
- 能申请哪类 terminal output lease；
- 能占用多少 memory / frame budget。

UI capability 与 OS sandbox 分离。即使插件进程有权访问文件，它也不自动有权读主
UI 的全部事件。

## 3. Frame 生命周期

v0.2 建议把 frame 生命周期明确成状态机：

```text
requested
  → planned
  → laid_out
  → encoded
  → queued
  → accepted
       ├─ not blocked → presented
       └─ blocked     → drained → presented

accepted 之前：可被更新 revision superseded
accepted 之前：布局 / 编码 / lease 失败则 aborted
```

### 3.1 语义

- `requested`：某个 lane 请求下一帧；
- `planned`：FrameClock 选择 lane、预算和 quality；
- `laid_out`：projection 产出不可变 FrameRecord；
- `encoded`：damage 已变成 ANSI bytes；
- `queued`：等待 terminal lease；
- `accepted`：`stdout.write()` 接受该批字节；
- `drained`：仅当写入返回 backpressure 时，底层 writable buffer 已排空；
- `presented`：buTUI 认为该 frame 已对用户可见；未阻塞时在 accepted 后立即进入，
  阻塞时在 drained 后进入；
- `superseded`：尚未 accepted 的旧 frame 被更新 revision 取代；
- `aborted`：布局、编码或 lease 获取失败。

只有 `presented` frame 可以成为鼠标 hit test 基准。键盘和 paste 可以针对当前
logical focus，因为用户输入的目标通常不应因一帧延迟而回退。

### 3.2 120fps 的含义

120fps 是目标调度率，不是“每个组件都必须在 8.33ms 内完成”。

建议预算：

```text
compute budget: 6.0ms p95
transport slack: 2.33ms
input budget:    1 frame, 不等待装饰动画
```

超过预算时按以下顺序降级：

1. 暂停 decorative animation；
2. 合并 reveal 的推进；
3. 降低 shimmer / scroll inertia 更新；
4. 延后 telemetry / debug hooks；
5. 最后才合并普通状态帧。

永远不降级：输入处理、最终内容一致性、终端恢复、基础焦点和鼠标语义。

## 4. Stream revision 模型

v0.1 的 `StreamSource` 只适合“只增文本 + volatile tail”。真实 agent 流还会出现：

- reasoning 被替换或取消；
- tool progress 和文本交错；
- Markdown tail 从未定稿变成 stable；
- 同一个 tool 的 diff 被重排；
- permission 状态改变后续内容的展示优先级。

因此 v0.2 的流单位不是字符串，而是：

```ts
interface StreamEnvelope {
  sessionId: string;
  streamId: string;
  seq: number;
  baseRevision: number;
  kind: "text" | "reasoning" | "tool" | "diff" | "status";
  op:
    | { type: "append"; delta: string }
    | { type: "replace-tail"; from: TailAnchor | null; text: string }
    | { type: "finish"; digest?: string }
    | { type: "cancel"; reason: string };
}

interface TailAnchor {
  lineId: LineId;
  grapheme: number;
}
```

核心规则：

1. 每个 `streamId` 只有一个逻辑 writer。
2. `seq` 必须连续；重复 op 可幂等忽略，缺口必须显式报告。
3. `replace-tail.from` 只能落在未定稿 tail；越界是错误，不能静默覆盖 stable 行。
4. `finish` 之后只能 replay，不能再 append。
5. `cancel` 保留 tombstone，避免重放时“消失的流”改变后续布局。
6. renderer 只消费 stream projection，不拥有业务流状态。
7. 全局顺序由 `session revision` 决定，单流顺序由 `seq` 决定。

`StreamLedger` 同时维护：

- `stable`: 可 spill、可压缩、可长期保留；
- `volatile`: 必须快速访问、通常只在内存；
- `tombstone`: 不渲染但参与重放；
- `cursor`: 每个 viewer 自己的可见位置，不写回 stream。

## 5. Terminal ownership 模型

### 5.1 Lease 类型

| Lease | 用途 | 可覆盖 | 必须恢复 |
|---|---|---:|---:|
| `frame` | buTUI 主帧 | 是 | 是 |
| `append` | console / 日志 / 外部文本 | 否 | 否 |
| `raw` | 子进程 PTY / 调试器 | 否 | 是 |
| `probe` | capability / 终端查询 | 否 | 否 |

### 5.2 仲裁规则

- 同一时刻只有一个 active owner；
- `raw` 优先于 `frame`，但必须等待当前 frame accepted；
- `frame` 可以 supersede 尚未 accepted 的同 owner frame；
- `append` 先于后续 frame，frame 在 append 后强制 full damage；
- `probe` 不能长期持有 lease；
- lease 过期必须释放，不能因插件崩溃永久锁住终端；
- 任何 `suspend` / `resume` 后，下一帧都是 full damage。

### 5.3 不可承诺的边界

JavaScript 无法阻止第三方库缓存 `process.stdout.write` 后绕过 arbiter。v0.2 的策略：

1. 所有 buTUI 内部路径必须走 arbiter；
2. 默认 patch `console` 和已知 app-level sink；
3. 对未知外部写入做 `externalWrite` 检测；
4. 检测到后清空 renderer previous state，强制 full redraw；
5. 文档明确“未走 arbiter 的写入是 best-effort 兼容，不是安全边界”。

## 6. Backpressure 与交互一致性

v0.1 已经做到：`write() === false` 时暂停自动绘制，`drain` 后只画最新状态。
v0.2 还要补齐“事件针对哪一帧”的一致性。

### 6.1 两条坐标线

```text
logical state line:  session revision → focus / semantic state
visual state line:   presented frame → mouse coordinates / hit test
```

- 键盘、paste、命令：走 logical state line；
- 鼠标 press / release / wheel：走 `lastPresentedFrame`；
- 拖动捕获：捕获节点 id + frame id，事件继续投递到捕获节点；
- 若节点已消失：尝试 semantic id，再退到 root，并标记 `stale: true`。

这样慢终端下不会出现“用户点到了屏幕上的 A，实际命中了内存中的 B”。

### 6.2 Frame version 绑定

每个输入事件在进入 runtime 时记录：

```ts
interface RoutedInput<T> {
  event: T;
  receivedAt: number;
  presentedFrameId: number;
  sessionRevision: number;
}
```

`send(event)` 的 v0.1 兼容入口会补齐这些字段。新入口可以显式指定 frame id，
用于 remote / replay / 测试。

### 6.3 Flush 语义

`waitUntilFrameFlushed(frameId)` 至少支持：

- `accepted`：`stdout.write()` 返回；
- `drained`：writable 触发 `drain`；
- `presented`：仅表示 buTUI 已提交，不声称终端物理刷新。

默认等待 `accepted`。退出、suspend、测试快照可以要求 `drained`。

## 7. 长会话内存模型

### 7.1 分类

| 状态 | 例子 | 可否丢弃 | 默认策略 |
|---|---|---:|---|
| live | 当前输入、焦点、lease | 否 | 常驻 |
| hot | 最近 transcript / stream | 否 | 内存 |
| warm | 已离开视口的 stable 内容 | 可重建 | 压缩 / 分段 |
| cold | 旧 transcript / replay | 可重建 | spill |
| ephemeral | frame、damage、ANSI buffer | 是 | 最多保留两帧 |

### 7.2 Admission control

每次 append 先向 `MemoryLedger` 申请预算：

```text
reserve(kind, bytes, priority) → grant | spill | reject
```

当预算不足时按顺序处理：

1. 丢弃 ephemeral；
2. 压缩 warm；
3. spill cold；
4. 降低 telemetry / replay snapshot 密度；
5. 最后才向应用报告 `MemoryPressure`。

不能自动丢失：未提交输入、未完成 tool 的权威状态、undo journal 的已承诺点、
terminal recovery journal。

### 7.3 平台期验收

长会话测试必须报告：

- heap used / rss；
- transcript logical bytes；
- hot / warm / cold bytes；
- spill 次数和压缩率；
- 每 10k 行的增量斜率；
- GC 后 retained bytes。

目标是：消息数增长时，热内存由 retention policy 限制，而不是靠 GC 碰运气。

## 8. 可重放 Session

### 8.1 真相格式

Session log 只记录有因果的事件，不记录 ANSI：

```ts
type SessionRecord =
  | InputRecord
  | ResizeRecord
  | StreamRecord
  | CapabilityRecord
  | ClockRecord
  | SnapshotRecord;
```

### 8.2 模式

| 模式 | 时间 | 用途 |
|---|---|---|
| `live` | 真实 clock | 正常终端 |
| `replay` | 记录时间 / 可缩放 | 问题复现 |
| `fast` | 立即推进 | CI / 回归 |
| `headless` | 虚拟 viewport | 语义快照 |
| `remote` | adapter clock | SSH / Web |

### 8.3 比较维度

不要只比较 ANSI bytes。终端能力、颜色和 synchronized output 会不同。至少比较：

1. semantic tree / region；
2. logical frame text；
3. damage line set；
4. stream final state；
5. input route result；
6. memory ledger peak。

## 9. Capability-bound Plugin UI

### 9.1 三类 capability

```ts
interface PluginCapabilities {
  slots: Set<SlotGrant>;
  events: Set<EventGrant>;
  commands: Set<CommandGrant>;
  output: OutputGrant;
  memory: MemoryGrant;
}
```

- `SlotGrant`：允许挂载的位置和尺寸类别；
- `EventGrant`：允许读取的 semantic region / event kind；
- `CommandGrant`：允许调用的 command id；
- `OutputGrant`：允许申请的 lease 类型；
- `MemoryGrant`：允许占用的 bytes / frame budget。

### 9.2 撤销语义

- revoke 立即让 token 失效；
- 后续事件不再投递；
- 已挂载 UI 收到 unmount；
- 已占用 output lease 强制释放；
- audit log 记录 grant / revoke / violation。

### 9.3 与 OS sandbox 的关系

```text
OS sandbox: 进程能不能读文件、发网络、创建子进程
UI capability: 插件能不能读事件、写区域、调命令
```

两者必须同时成立，不能互相替代。Bun spawn sandbox hook 只解决后者的一半；
插件默认仍应运行在 child process + IPC 中。

## 10. 建议的包与模块边界

不立即拆很多 package。先在现有 package 内部建立边界，稳定后再公开。

```text
@butui/core
  frame-clock/      FrameClock, lane, budget, deterministic clock
  node / events / focus / theme

@butui/runtime
  session/          SessionKernel, snapshot, replay
  projection/       projection adapter, frame records
  transport/        Damage, TerminalArbiter
  control/          AdaptiveQuality, MemoryLedger
  capabilities/     CapabilityBroker

@butui/stream
  ledger/           StreamEnvelope, StreamLedger, compaction
  adapters/         v0.1 StreamSource adapter

@butui/terminal
  lease/            TerminalLease, TerminalModeJournal
  capability/       terminal capability probe
```

FrameClock 放在 core 而不是 runtime，因为 `@butui/solid` / `@butui/stream` 也要
订阅它；runtime 已依赖 solid，反向导入会形成循环。

Public API 只在边界稳定后提升。内部先做 adapter，避免 v0.2 原型污染 v0.1 的
`createTuiApp` 契约。

## 11. v0.1 兼容路径

v0.2 不替换 v0.1，而是让 v0.1 成为 v0.2 的一个 projection adapter。

```ts
// 旧入口继续工作
const app = createTuiApp({ view, render: { mode: "frame", fps: 120 } });

// 新入口逐步暴露能力
const runtime = createSessionRuntime({
  session,
  projection,
  transport,
  quality,
  memory,
});
```

迁移顺序：

1. `RenderScheduler` 接入统一 FrameClock，行为不变；
2. `TerminalSession` 接入 TerminalArbiter，保持 `write()` 签名；
3. `StreamSource` 增加 `StreamEnvelope` adapter；
4. `createTuiApp` 可选启用 frame version / lease / memory；
5. 新 `createSessionRuntime` 成为 replay / remote / plugin 的主入口。

## 12. 原型顺序与停止条件

### P0 原型

1. **FrameClock + FrameTicket（核心已完成）**
   - 证明输入不被 reveal / animation 阻塞；
   - fake clock 下确定性。
2. **TerminalArbiter（核心与 TerminalSession adapter 已完成）**
   - frame / append / raw / probe lease 与优先级队列已实现；
   - frame stale / superseded、append full damage、raw suspend/resume 已实现；
   - blocked drain 与 TTL revoke 已实现；
   - TerminalSession 已在 start/stop 获取和释放 frame lease，write 保持兼容；
   - runtime 已传 frameId，并提供 suspend/resume + full damage；
   - `withRawLease` 已封装 raw acquire / release / frame resume，并提供 raw stdin 路由；
   - `runPtyWithRawLease` 已基于 Bun.Terminal / Bun.spawn 接入子进程 PTY，并转发 resize；
3. **Presented-frame hit test（基础已完成）**
   - 慢 stdout 下点击仍命中用户看到的 frame；
   - local bounds、capture、resize invalidation 已接入；
   - 节点消失时支持 semantic 唯一回退 / root `stale`，不误触同坐标新节点；
   - 新 frame presented 后会按静止指针重算 hover；
   - presented selection 固定起始 frame，release 使用快照文本。
4. **StreamLedger（核心与 adapter 已完成）**
   - append / replace-tail / finish / cancel 已可重放；
   - gap / duplicate / conflict / stable-line 越界均显式处理；
   - `StreamSource` adapter 已接 push / flush，并保留可选 replace-tail / cancel；
   - compaction 与 spill 待下一步。
5. **MemoryLedger（核心已完成）**
   - reservation / resize / release 与 class / pinned 限额已实现；
   - 预算不足时返回 spill candidates，不静默降级；
   - StreamLedger 的 append / replace-tail 已接入 allocation admission；
   - SpillStore / StreamRetention 已实现按字节保留与可恢复 manifest；
   - `applyWithSpill()` 会自动 spill 旧 stable line、释放 reservation 并重试；
   - spilled line 已 compact 成范围 segment，hot lines 在 100k 行下稳定约 4.2k；
   - FileSpillStore 已实现 numeric chunk index、批量写/校验、流式 reopen / compact；
   - numeric chunk 可配置磁盘 sidecar + LRU；1M 行 heap 17.5MB → 8.34MB；
   - full hydrate 已按 1,024 条有界批读；1M 条恢复约 3.0s，但仍会 materialize 全量；
   - `readCold()` 支持 viewport 定点读取且不改变 ledger；1M 中随机 1,000 条约 5ms；
   - `readStableRange()` 已按逻辑 stable-line 下标混合冷热窗口；跨边界 1,000 行约 2.2ms；
   - `StreamLedger.applied` 使用 chunked typed arrays，保留完整 duplicate/conflict 历史；
   - applied chunk 可配置磁盘 sidecar + LRU；10M 事件 heap 增量约 0.18MB，索引文件约 50MB；
   - 5M 空 append 实测约 2.16M events/s、5.28 bytes/event，旧 seq duplicate 仍正确；
   - `createStreamWindow()` 已适配为只读 `StreamSource`，窗口滚动带 stale-request 防护；
   - 窗口 source 使用 revision-aware LRU，重复窗口不重复 cold read，`refresh()` 可绕过缓存；
   - `createStreamWindowController()` 统一 height / offset / page，并按 viewport page 对齐预取；
   - 注入 FrameClock 后同一帧只提交最新滚动 offset，避免连续滚轮逐事件触发 cold read；
   - 1M 行 / 20k 滚动事件实测：1,000 帧、500 次 cold read、frame p95 约 0.78ms；
   - 键盘 / 滚轮与 ScrollBar adapter 已接入同一 controller；
   - `<StreamWindow>` 已组合窗口、输入与可选 ScrollBar，支持响应式 height；
   - `createTuiApp + FakeTerminal` 已验证 wheel → FrameClock → cold read → presented frame；
   - 多 stream 交错 LineId 使用 `lineRuns`；4 路 92,720 条 spilled line 压缩为
     4 个 segment / 4 个 run / 0 个显式 lineId；
   - 1M 行实测约 10.5s、heap 约 17.5MB、cold file 145.6MB；剩余约
     9 bytes/line 的直接寻址索引，详见 `V0.2_RETENTION_REPORT.md`。

### 停止条件

- 如果 FrameClock 没有改善输入 p95，不继续扩展优先级系统；
- 如果 TerminalArbiter 不能在不 patch stdout 的情况下处理主流路径，先缩小承诺；
- 如果 StreamLedger 的复杂度超过 bugent 实际协议，退回 append + replace-tail 两 op；
- 如果没有真实 PTY profile 证明内存压力，不引入 SQLite / 压缩后端；
- 任何原生加速都必须先通过 v0.2 的 2× / 20% frame budget 门槛。

## 13. 明确不做

v0.2 Frontier 不做：

- Rust / Zig 重写 runtime；
- fork Bun 只为 TUI 增加 API；
- 多终端实时协作；
- 精确回读终端真实 scrollback；
- GPU / 原生窗口后端；
- 为了 feature parity 堆组件；
- 把所有业务状态强制搬进 buTUI store；
- 声称能拦截所有未知 stdout writer。

## 14. 最终判断

v0.2 的“超常道路”不是换语言，而是改变系统边界：

```text
Ink / OpenTUI 主要回答：
  如何把 React / Renderable 可靠地画到终端？

buTUI v0.2 还要回答：
  当 UI 是一个长期、可重放、可被插件扩展、被慢终端背压、
  并且要跨 TUI / Web / SSH 保持一致的 agent session 时，
  谁拥有状态、谁拥有终端、谁为内存和交互一致性负责？
```

如果这个问题答对了，Rust、WASM、OpenTUI 兼容都只是可替换的执行后端；
如果答错了，即使底层全用 native，也只会得到一个更快但不稳定的终端 UI。
