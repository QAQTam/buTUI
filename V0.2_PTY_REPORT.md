# buTUI v0.2 PTY Report

> 日期：2026-09-24  
> 环境：Linux x64 / Bun 1.4.3-canary.1  
> 脚本：`scripts/transcript-pty-bench.tsx`

## 1. 目标

在真实 PTY 中验证：

- `createTuiApp` 的 frame scheduler 是否持续工作；
- `StreamWindow` 是否同时消费 stable lines 与 volatile tail；
- 2000 chunk/s 下输入到 drained 的延迟；
- 真实 stdout 是否出现未恢复的 backpressure。

本报告使用合成 transcript，不替代 bugent 真实业务 profile。

## 2. 命令

```bash
bun --conditions=browser run scripts/transcript-pty-bench.tsx \
  --seconds=2 \
  --chunks-per-second=2000
```

父进程创建 `Bun.Terminal`（100×32），子进程在 PTY 内运行
`createTuiApp + StreamWindow`。子进程输出指标到临时文件，父进程汇总。

## 3. 结果

| 指标 | 结果 |
|---|---:|
| PTY | 100×32 |
| 持续时间 | 2.0s |
| chunk 速率 | 2,000/s |
| 实际 chunks | 3,880 |
| revision | 485 |
| 绘制帧 | 204 |
| 平均绘制帧率 | 约 102fps |
| 输出 bytes | 31,235 |
| blocked frame | 0 |
| input→drained p50 | 4.56ms |
| input→drained p95 | 10.65ms |
| input→drained max | 10.65ms |
| stable lines | 242 |
| volatile tail | 1 |

## 4. Damage span 对比

同一脚本在 line repaint 与 cell span damage 下的两次 2 秒运行：

| 版本 | 输出 bytes | frame bytes | input→drained p95 |
|---|---:|---:|---:|
| line repaint | 31,364 | 31,235 | 10.65ms |
| span damage | 12,517 | 12,388 | 9.85ms |

PTY 输出量约降至 **40%**；帧数仍约 102fps，未出现 blocked frame。两次运行
的 chunk 数分别为 3,880 / 3,872，属于调度抖动，不是吞吐回归。

## 5. 发现并修复的帧内核 bug

首次真实 PTY 运行只绘制了 2 帧。根因不是终端背压，而是 `FrameClock`：

- timer 可能因时钟精度早于 deadline 触发；
- `runDue()` 发现没有到期请求后直接返回；
- 队列中的 request 永久没有重新挂 timer。

修复：`runDue()` 在没有 due request 时调用 `scheduleWake()`。回归测试加入
`tests/frame-clock.test.ts`，覆盖“timer 早于 deadline 触发”。

修复后同一负载稳定绘制 204 帧，且无 blocked frame。

## 6. 真实消费者接线

`examples/agent-demo` 新增 `createTranscript()`：

```text
AgentEvent
  → StreamLedger append / retention
  → stable lines + volatile tail
  → <StreamWindow follow revision>
```

默认仍显示语义化 `AgentView`；设置
`BUTUI_TRANSCRIPT_MODE=window` 切换到 ledger-backed transcript 窗口。

## 7. 边界

- 当前是合成 token 负载，不是 bugent 真实 transcript。
- `input→drained` 只证明 writable 缓冲排空，不等于物理屏幕已刷新。
- 尚未覆盖长会话 heap 平台期、真实工具输出和外部 stdout 混写。
