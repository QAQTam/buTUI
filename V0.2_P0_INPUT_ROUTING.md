# v0.2 P0：Presented-frame 输入路由

> 前置：[V0.2_FRONTIER.md](./V0.2_FRONTIER.md)  
> 契约：[V0.2_CONTRACTS.md](./V0.2_CONTRACTS.md) §3.5 / §4  
> 目标：慢终端、resize、流式布局变化时，鼠标始终针对**用户实际看到的帧**命中，
> 而不是针对内存中的最新 layout。
>
> 当前原型：`PresentedFrameStore` 已实现；runtime 已提供
> `inputRouting: "presented"`，覆盖 hit target、local bounds、capture 和 resize
> invalidation。semantic stale fallback、hover 重算和 selection frame snapshot
> 仍待下一阶段。

## 0. 设计结论

1. 键盘 / paste 走 logical state line。
2. 鼠标 press / release / move / wheel 走 visual state line。
3. `lastPresentedFrame` 是鼠标 hit test 的唯一默认基准。
4. accepted 但尚未 presented 的 frame 不能替换 hit test 基准。
5. 节点消失时先按 semantic id 回退，再退 root，并标记 `stale=true`。
6. resize 后旧 presented frame 立即失效，直到下一帧 full damage presented。
7. v0.1 默认保持 logical routing；v0.2 runtime 默认 presented routing。
8. replay 日志必须记录输入到达时的 presented frame id，否则无法精确重放路由。

## 1. 两条坐标线

```text
logical state line:
  input → reducer → focus → semantic tree → next projection

visual state line:
  presented frame → terminal coordinates → hit test → target node
```

两条线会在事件派发时汇合，但不能互相替代：

- 键盘“回车”应作用于当前焦点，即使焦点改变尚未画出来；
- 鼠标点击必须作用于用户看到的按钮，即使内存中的按钮已经移动；
- paste 应作用于当前焦点，并遵循当前权限 / 输入状态；
- wheel 通常走 visual line，因为滚轮目标是用户看到的滚动区。

## 2. PresentedFrame

```ts
interface PresentedFrame {
  frameId: FrameId;
  sessionRevision: SessionRevision;
  projectionRevision: ProjectionRevision;
  presentedAt: number;
  layout: Frame;
  index: PresentedFrameIndex;
}

interface PresentedFrameIndex {
  hit(x: number, y: number): HitResult | undefined;
  boundsOf(nodeId: number): Rect | undefined;
  semanticBounds(semantic: string): Rect | undefined;
}

interface HitResult {
  nodeId?: number;
  semantic?: string;
  bounds?: Rect;
  localX?: number;
  localY?: number;
}

interface Rect {
  x: number;
  y: number;
  width: number;
  height: number;
}
```

`PresentedFrameIndex` 可以从现有 `Frame` 懒构建，不要求 layout 新增公共 API。

要求：

- 索引最多缓存一个 presented frame；
- frame 被替换后旧索引可在 capture / selection 仍引用时短暂保留；
- 不保留无界历史 frame；
- semantic id 在同一 frame 内必须唯一，否则启动时报告开发错误。

## 3. 输入记录

```ts
interface InputRoute {
  presentedFrameId: FrameId;
  sessionRevision: SessionRevision;
  receivedAt: number;
  stale: boolean;
}

interface RoutedInput<T extends ButuiEvent> {
  event: T;
  route: InputRoute;
  hit?: HitResult;
}
```

`send()` 的 v0.1 兼容入口会补齐 route。新入口允许显式传入：

```ts
runtime.send(event, {
  presentedFrameId,
  sessionRevision,
  receivedAt,
});
```

replay / remote / 测试必须使用显式 route，不能依赖当前墙钟或实时 terminal。

## 4. 路由表

| Event | 默认基准 | target 解析 | 备注 |
|---|---|---|---|
| key | logical focus | focus node | 不因未 presented 状态回退 |
| paste | logical focus | focus node | 权限 / 输入状态按当前 session |
| mouse press | presented frame | hit test | 可触发 focus / capture |
| mouse release | capture 或 presented frame | capture node / hit test | capture 优先 |
| mouse move | capture 或 presented frame | capture node / hit test | hover 只针对 presented |
| wheel | presented frame | hit test | 若 stale，语义回退后派发 |
| drag | capture | capture node | 使用 capture bounds |
| resize | system | 无 target | 使 presented frame 失效 |

## 5. 鼠标路由算法

```ts
function routeMouse(event: MouseEvent): RoutedInput<MouseEvent> {
  if (presented === undefined) {
    return routeWithoutPresentation(event, "before-first-present");
  }

  const hit = presented.index.hit(event.x, event.y);
  const resolved = resolveTarget(hit, presented);

  return {
    event,
    route: {
      presentedFrameId: presented.frameId,
      sessionRevision: presented.sessionRevision,
      receivedAt: now(),
      stale: resolved.stale,
    },
    hit: resolved.hit,
  };
}
```

### 5.1 无 presented frame

可能场景：

- 启动后第一帧尚未完成；
- resize 后 full frame 尚未 presented；
- suspend / raw lease 尚未恢复；
- renderer 被 invalidate。

策略：

- 第一次启动前：允许使用当前 layout 建立首帧，但标记 `before-first-present`；
- resize / suspend 后：不猜测坐标，路由到 root，并标记 `stale`；
- 对需要精确点击的组件，应用可选择丢弃 stale click，而不是误触。

## 6. Target resolution

优先级：

1. capture node；
2. presented frame 的 node id；
3. presented frame 的 semantic id；
4. 当前 projection 中同 semantic id 的唯一节点；
5. root。

### 6.1 节点仍存在

- 使用 presented frame 的 bounds 计算 localX / localY；
- 即使节点在新 layout 中移动，也按用户看到的旧 bounds 计算；
- 事件 target 是当前 node 对象，但 route 保留旧 frame id；
- `stale=false`。

### 6.2 节点已删除

```ts
interface ResolvedTarget {
  target: Node;
  hit: HitResult;
  stale: boolean;
}
```

流程：

1. `nodeById(root, hit.nodeId)` 失败；
2. 若 hit.semantic 存在，查找当前树中唯一 semantic node；
3. 找到则派发到该节点，`stale=true`；
4. 找不到则派发 root，`stale=true`；
5. 不自动命中同坐标的新节点。

第 5 条很重要：用户点的是旧 UI，新坐标上的 B 按钮不能吃掉 A 的点击。

### 6.3 semantic 回退

semantic id 用于跨重排保持意图：

```text
tool:call-1
message:m2
permission:req-7
```

- 相同 semantic 仍存在：可以安全回退；
- semantic 重复：开发错误，选择第一个是危险的，默认报告并退 root；
- semantic 已不存在：root + stale。

## 7. Mouse capture

```ts
interface MouseCapture {
  nodeId: number;
  semantic?: string;
  frameId: FrameId;
  bounds: Rect;
  startX: number;
  startY: number;
  released: boolean;
}
```

规则：

1. press 命中后，组件调用 `capture(node)`；
2. capture 保存当时的 frame id 和 bounds；
3. move / release 优先派发到 capture node；
4. localX / localY 使用 capture bounds，不使用最新 layout；
5. capture node 删除时释放 capture，并派发 `dragcancel`；
6. resize 时强制取消 capture；
7. release 后立即释放 capture；
8. 旧 presented frame 被替换不影响 capture 坐标。

这解决当前 v0.1 在拖动过程中因 streaming layout 变化导致 localX / localY 漂移的
问题。

## 8. Hover

终端只在鼠标移动时发事件。若鼠标静止但 frame 更新，目标可能变化。

策略：

```ts
interface PointerState {
  x: number;
  y: number;
  lastPresentedFrameId: FrameId;
  hoveredNodeId?: number;
  hoveredSemantic?: string;
}
```

每次新 frame presented：

1. 若 pointer mode 不是 hover，跳过；
2. 用 pointer 坐标对**新 frame** hit test；
3. 目标变化时派发 leave → enter；
4. 目标相同但 semantic 变化时，视为变化；
5. 目标节点消失时，派发 leave，不自动 enter 同坐标新节点；
6. hover 重新计算不改变 pointer 坐标。

如果没有真实 hover mode，不主动合成 hover；保留 v0.1 行为。

## 9. Click count

双击 / 三击合成键：

```ts
interface ClickKey {
  button: MouseEvent["button"];
  targetKey: string;      // semantic ?? nodeId
  x: number;
  y: number;
}
```

规则：

- 时间阈值和空间阈值沿用 v0.1；
- `targetKey` 不同则重置；
- frameId 变化不自动重置；
- semantic 相同但节点重建时仍可连续点击；
- stale click 不参与双击合成；
- 右键 context menu 不参与左键 click count。

## 10. Selection

v0.2 的选区必须带 frame 版本，否则 streaming 后坐标会指向不同文本。

```ts
interface SelectionAnchor {
  frameId: FrameId;
  x: number;
  y: number;
  nodeId?: number;
  semantic?: string;
}
```

P0 策略：

1. press 记录 anchor frame id；
2. 拖动期间固定该 presented frame 的文本快照；
3. move 只更新 focus 坐标；
4. release 从固定快照提取文本；
5. frame 被替换但选择未结束时，旧 frame snapshot 最多保留到 release；
6. resize / suspend 取消选择，避免坐标失真；
7. 选择完成后不保留 frame；
8. 流式内容在拖动期间变化不改变本次选择文本。

这是一种“所见即所选”语义：用户选的是看到的文本，而不是释放瞬间已经变化的内容。

## 11. Resize 与 suspend

### resize

1. 收到 SIGWINCH；
2. 立即将 `presented` 标记为 invalid；
3. 取消 selection / capture；
4. 生成 full damage frame；
5. 新 frame presented 前，鼠标只做 stale / root 路由。

### raw lease / suspend

1. suspend 前等待当前 frame accepted；
2. 停止 frame writes；
3. 使 presented frame 失效；
4. 子进程退出 / resume；
5. full damage；
6. 新 frame presented 后恢复鼠标语义。

## 12. Replay

输入日志不能只存 `MouseEvent`：

```ts
interface InputRecord {
  id: string;
  seq: number;
  at: number;
  event: ButuiEvent;
  route: {
    presentedFrameId: FrameId;
    sessionRevision: SessionRevision;
  };
}
```

Replay 要求：

1. replay 的 frame id 必须与记录一致；
2. 若 frame id 不匹配，立即报 replay divergence；
3. 不使用 replay 当前时间重新路由；
4. headless replay 可以跳过 ANSI，但不能跳过 presented frame；
5. stale 决策必须进入 replay diff；
6. route 记录不能包含 Node 对象引用。

## 13. v0.1 兼容

新增选项：

```ts
interface TuiAppOptions {
  inputRouting?: "logical" | "presented";
}
```

默认：

- `createTuiApp`：`logical`，保持 v0.1 行为；
- `createSessionRuntime`：`presented`；
- `mount()` / headless test helper：`logical`；
- 显式设置 `presented` 时，测试可以验证 v0.2 语义。

兼容路径：

1. `logical`：每次事件用 `computeFrame()`，保持当前实现；
2. `presented`：使用 `lastPresentedFrame`；
3. `presented` 在首帧前使用 bootstrap frame；
4. 两种模式共享同一 target resolution 和 stale 字段；
5. v0.1 测试不修改，v0.2 新测试覆盖 presented。

## 14. 测试矩阵

### 14.1 基本 hit test

- frame A presented，点击 A 的按钮命中 A；
- frame B 已在内存但未 presented，点击仍命中 A；
- frame B presented 后点击命中 B；
- localX / localY 相对 presented bounds。

### 14.2 stale

- 旧节点删除，同坐标出现新按钮；
- 点击不命中新按钮，route 到 root 或 semantic 回退；
- `stale=true` 可被应用观察；
- stale click 不触发 double click。

### 14.3 capture

- capture 后 layout 变化，move 仍派发 capture node；
- local 坐标保持稳定；
- capture node 删除派发 dragcancel；
- resize 强制取消 capture。

### 14.4 hover

- 鼠标静止，新 frame 改变目标，合成 leave / enter；
- 目标删除只 leave；
- 同坐标新节点不自动 enter；
- non-hover mode 不合成。

### 14.5 selection

- 拖动期间流式更新，选中文本保持“所见即所选”；
- frame 替换不改变 anchor；
- resize 取消 selection；
- 选择结束后旧 frame snapshot 被释放。

### 14.6 backpressure

- write(false) 后 state 变化，鼠标仍命中旧 presented frame；
- drain 后最新 frame presented，鼠标切到新基准；
- 旧 frame 不参与点击；
- 退出 / suspend 不留下 capture / selection。

### 14.7 replay

- 同 route 日志重放出相同 target / stale / delivered 结果；
- frame id 不一致时明确失败；
- headless replay 不依赖真实终端。

## 15. 待验证决策

- `before-first-present` 是否允许 bootstrap hit test，还是必须丢弃点击？
- semantic 重复时是启动报错、退 root，还是按注册顺序取第一个？
- selection 固定旧 frame 是否应作为默认，还是提供 `live` 选择模式？
- hover 重算是否应该在 accepted 还是 presented 后执行？
- stale click 是否应默认丢弃，而不是派发 root？
- capture bounds 是否应存储为 frame snapshot，还是只存 Rect？

P0 原型先实现“presented + stale + capture bounds”三条，其余策略由真实鼠标
体验和 bugent 权限弹窗测试决定。
