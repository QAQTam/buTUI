# buTUI v0.2 Contracts

> 配套：[V0.2_FRONTIER.md](./V0.2_FRONTIER.md)  
> P0 细化：[V0.2_P0_FRAME_CLOCK.md](./V0.2_P0_FRAME_CLOCK.md) /
> [V0.2_P0_INPUT_ROUTING.md](./V0.2_P0_INPUT_ROUTING.md)
>
> 本文定义**可评审的原型契约**，不是已冻结的公共 API。
> 目的：让 FrameClock、TerminalArbiter、StreamLedger、MemoryLedger 可以分别
> 原型化，并通过同一组不变量验收。

## 0. 约定

- `Revision` 是 session 级单调逻辑时钟，不等于 wall-clock。
- `FrameId`、`LeaseId`、`StreamId`、`LineId`、`CapabilityId` 都是 opaque id。
- 所有 `Promise` 接口都必须有取消 / dispose 语义。
- 本文的 TypeScript 只表达形状和所有权，不要求逐字实现。
- v0.1 公共 API 不改名、不改默认行为；v0.2 通过 adapter 渐进接入。

## 1. ID 与 revision

```ts
type SessionId = string & { readonly __sessionId: unique symbol };
type StreamId = string & { readonly __streamId: unique symbol };
type LineId = string & { readonly __lineId: unique symbol };
type CapabilityId = string & { readonly __capabilityId: unique symbol };

type SessionRevision = number & { readonly __sessionRevision: unique symbol };
type ProjectionRevision = number & { readonly __projectionRevision: unique symbol };
type FrameId = number & { readonly __frameId: unique symbol };
type LeaseId = number & { readonly __leaseId: unique symbol };

interface CausalRef {
  sessionId: SessionId;
  revision: SessionRevision;
  recordId?: string;
}
```

规则：

1. `SessionRevision` 只增；同一个 revision 不可改写。
2. `ProjectionRevision` 表示 projection 已消费到的 session revision。
3. `FrameId` 只增；FrameId 不要求与 revision 一一对应。
4. 跨进程日志必须同时记录 `sessionId + revision`，不能只记局部序号。
5. wall-clock 只用于超时 / 诊断，不用于 reducer 决策。

## 2. FrameClock

### 2.1 Lane

```ts
type FrameLane =
  | "input"        // 同步处理，不等待绘制
  | "critical"     // 焦点、布局、状态提交
  | "reveal"       // 文本平滑显现
  | "decorative"   // shimmer、惯性、装饰动画
  | "maintenance"; // telemetry、压缩、debug hook

type QualityLevel = "full" | "balanced" | "responsive" | "minimal";
type ScheduledFrameLane = Exclude<FrameLane, "input">;

type ClockFrameOutcome =
  | { status: "presented"; frameId: FrameId; at: number }
  | { status: "blocked"; frameId: FrameId; at: number }
  | { status: "drained"; frameId: FrameId; at: number }
  | { status: "superseded"; frameId: FrameId; at: number }
  | { status: "aborted"; frameId: FrameId; at: number; reason: string };

interface FrameClock {
  now(): number;

  /**
   * 申请下一次可绘制帧。
   * input lane 同步处理，不进入 FrameRequest 队列。
   */
  request(input: FrameRequest): FrameRequestHandle;

  cancel(handle: FrameRequestHandle): void;

  /** transport 报告 frame 生命周期结果。 */
  settle(outcome: ClockFrameOutcome): void;

  /** 测试 / replay 使用虚拟时间推进。 */
  advanceTo(time: number): void;

  stats(): FrameClockStats;
}

interface FrameRequest {
  lane: ScheduledFrameLane;
  reason: string;
  sessionRevision: SessionRevision;
  deadline?: number;
  coalesceKey?: string;
  work: (tick: FrameTick) => FrameWorkResult;
}

interface FrameTick {
  clockTime: number;
  frameId: FrameId;
  quality: QualityLevel;
  budgetMs: number;
  sessionRevision: SessionRevision;
}

type FrameWorkResult =
  | { status: "idle" }
  | { status: "dirty"; revision: SessionRevision }
  | { status: "budget-exceeded"; phase: string };
```

### 2.2 调度规则

1. `input` 不排队等待 120fps；输入处理在同一 JS turn 完成。
2. 同一 `coalesceKey` 只保留最新请求。
3. `critical` 每个 frame 最多执行一次。
4. `reveal` 可以被 critical supersede，但必须保留尾帧。
5. `decorative` 在 `responsive` / `minimal` 下暂停。
6. `maintenance` 只能在帧预算剩余时执行，不得阻塞输入。
7. `advanceTo()` 必须产生确定性结果，不能读取真实时间。
8. 同一个 tick 中多个 work 修改同一 projection 时，以最新 revision 为准。

### 2.3 降级顺序

```text
full
  ↓ p95 frame > 8.33ms 或 event-loop delay 连续超阈值
balanced
  ↓ p95 frame > 16.67ms 或 stdout blocked
responsive
  ↓ 用户 reduced-motion / 极端 backpressure / CPU 饱和
minimal
```

升档必须比降档慢：建议连续 2 秒满足预算才升一级，避免振荡。

### 2.4 不变量

- `input` 的完成不依赖任何绘制 Promise。
- 同一个 `FrameId` 不能跨 tick 复用。
- `advanceTo(t)` 两次，相同请求序列必须得到相同 frame 序列。
- 超过预算时不得静默丢弃 critical 工作；必须留下 dirty revision。

## 3. Frame 与 Damage

### 3.1 Ticket

```ts
interface FrameTicket {
  frameId: FrameId;
  sessionRevision: SessionRevision;
  projectionRevision: ProjectionRevision;
  lane: FrameLane;
  quality: QualityLevel;
  viewport: {
    columns: number;
    rows: number;
    cellWidthPx?: number;
    cellHeightPx?: number;
  };
  capabilities: TerminalCapabilities;
  requestedAt: number;
  deadline?: number;
  reason: string;
}
```

### 3.2 Damage

```ts
type Damage =
  | { kind: "full"; reason: string }
  | { kind: "lines"; from: number; to: number }
  | {
      kind: "spans";
      rows: ReadonlyArray<{
        y: number;
        from: number;
        to: number;
      }>;
    }
  | {
      kind: "graphics";
      rects: ReadonlyArray<{ id: string; x: number; y: number; width: number; height: number }>;
    };
```

`Damage` 是提示，不是安全边界。renderer 可以做更多检查，但不能把未 damage 的
内容误认为一定没变。`full` 必须覆盖 resize、resume、external write、renderer
reset、terminal capability 变化。

### 3.3 FrameRecord

```ts
type FrameState =
  | "planned"
  | "laid-out"
  | "encoded"
  | "queued"
  | "accepted"
  | "drained"
  | "presented"
  | "superseded"
  | "aborted";

interface FrameRecord {
  ticket: FrameTicket;
  state: FrameState;
  damage: Damage;
  layout: Frame;               // v0.1 @butui/layout Frame
  bytes?: Uint8Array;          // encoded ANSI
  acceptedAt?: number;
  drainedAt?: number;
  presentedAt?: number;
  error?: Error;
}
```

### 3.4 状态机

```text
planned → laid-out → encoded → queued → accepted
                                           ├─ not blocked → presented
                                           └─ blocked → drained → presented

accepted 之前：planned / laid-out / encoded / queued → superseded
accepted 之前：任意阶段失败 → aborted
```

转移规则：

- `accepted` 之前可以由更高 revision 取代；
- `accepted` 之后不可撤回 bytes；
- `write()` 返回 false 时，必须等 drain 后才能进入 `presented`；
- `write()` 返回 true / undefined 时，accepted 后直接进入 `presented`，不等待 drain；
- 只有 `presented` frame 能更新 `lastPresentedFrame`；
- `aborted` / `superseded` frame 必须释放 layout / bytes 引用。

### 3.5 Hit test 契约

```ts
interface PresentedFrame {
  frameId: FrameId;
  sessionRevision: SessionRevision;
  projectionRevision: ProjectionRevision;
  layout: Frame;
  presentedAt: number;
  index: PresentedFrameIndex;
}

interface PresentedFrameIndex {
  hit(x: number, y: number): HitResult | undefined;
  boundsOf(nodeId: number): Rect | undefined;
  semanticBounds(semantic: string): Rect | undefined;
}

interface Rect {
  x: number;
  y: number;
  width: number;
  height: number;
}

interface InputRoute {
  presentedFrameId: FrameId;
  sessionRevision: SessionRevision;
  receivedAt: number;
  stale: boolean;
}

interface HitResult {
  nodeId?: number;
  semantic?: string;
  bounds?: Rect;
  localX?: number;
  localY?: number;
}

interface RoutedInput<T extends ButuiEvent> {
  event: T;
  route: InputRoute;
  hit?: HitResult;
}
```

- 鼠标坐标只对 `PresentedFrame.layout` 做 hit test。
- 键盘 / paste 走 logical focus。
- 捕获拖动时，`PresentedFrame` 更新不影响 capture target。
- 若 target node 已删除：按 semantic id 查找，再退到 root，并设置 `stale=true`。

## 4. TerminalArbiter 与 TerminalLease

### 4.1 类型

```ts
type LeaseKind = "frame" | "append" | "raw" | "probe";
type LeaseState = "pending" | "active" | "suspended" | "released" | "revoked";

interface LeaseRequest {
  owner: string;
  kind: LeaseKind;
  reason: string;
  priority?: number;
  ttlMs?: number;
  signal?: AbortSignal;
}

interface TerminalLease {
  id: LeaseId;
  owner: string;
  kind: LeaseKind;
  state: LeaseState;
  acquiredAt: number;
  expiresAt?: number;
  capabilities: ReadonlySet<TerminalCapability>;
}

interface TerminalArbiter {
  acquire(request: LeaseRequest): Promise<TerminalLease>;
  write(lease: TerminalLease, batch: WriteBatch): WriteReceipt;
  suspend(lease: TerminalLease, reason: string): Promise<void>;
  resume(lease: TerminalLease): Promise<void>;
  release(lease: TerminalLease): Promise<void>;
  revoke(lease: TerminalLease, reason: string): void;
  current(): TerminalLease | undefined;
  onEvent(listener: (event: ArbiterEvent) => void): () => void;
}
```

### 4.2 WriteBatch

```ts
interface WriteBatch {
  kind: "frame" | "append" | "control";
  frameId?: FrameId;
  bytes: Uint8Array;
  replaces?: FrameId;
  barrier?: boolean;
  flush?: "accepted" | "drained";
}

interface WriteReceipt {
  accepted: boolean;
  blocked: boolean;
  bytesWritten: number;
  acceptedAt?: number;
  drained?: Promise<void>;
  rejectedReason?:
    | "not-owner"
    | "suspended"
    | "stale-frame"
    | "superseded"
    | "closed";
}
```

`TerminalSession.withRawLease()` 向 callback 提供：

```ts
interface RawLeaseContext {
  lease: TerminalLease;
  write(bytes: string | Uint8Array): boolean;
  onInput(listener: (chunk: Uint8Array) => void): () => void;
}
```

`runPtyWithRawLease()` 在 raw lease 内创建 `Bun.Terminal`、`Bun.spawn` 子进程，
转发子进程输出到宿主终端，并把 raw 期间 stdin 字节直接写入 PTY，绕过 UI decoder。
宿主 resize 会同步 `terminal.resize(cols, rows)`。`TerminalSession.runPty()` 与
`TuiApp.runPty()` 暴露同一能力，并自动管理 frame suspend / resume。
`PtyRunOptions.signal` abort 时会终止子进程并走同一清理路径。进入 / 退出 raw
模式会 reset InputDecoder，避免半截 escape / paste 跨模式污染。

### 4.3 Lease 状态机

```text
pending → active → suspended → active → released
   │        │           │
   └────────┴───────────┴────────────→ revoked
```

规则：

1. 任意时刻最多一个 active owner。
2. `frame` lease 可由同 owner 的新 frame supersede，但只能替换未 accepted 的 frame。
3. `control` bytes 永远不可丢弃；必须排在相邻 frame 之前或之后，由 `barrier` 指定。
4. `append` 到达后，下一个 frame 必须是 full damage。
5. `raw` 获取前必须等待当前 frame accepted；拿到后 renderer 停止写。
6. `raw` release 后下一帧 full damage，并重新探测 cursor / mouse / keyboard mode。
7. `ttlMs` 到期强制 revoke；插件崩溃不能锁死终端。
8. arbiter 不知道的 direct stdout write 只能 best-effort 检测。

### 4.4 Flush 语义

```ts
interface FrameBarrier {
  frameId: FrameId;
  accepted(): Promise<void>;
  drained(): Promise<void>;
}
```

- `accepted`：Node writable 接受了 bytes。
- `drained`：writable buffer 降到阈值以下。
- `presented`：buTUI 已提交事务，但不等于终端物理刷新。
- 默认 `waitUntilFrameFlushed()` 等待 `accepted`。
- 退出 / suspend / 测试快照可以等待 `drained`。

## 5. StreamLedger

### 5.1 Anchor

不要用裸 UTF-16 offset 表示 tail 替换位置。使用稳定行 id + grapheme 索引：

```ts
interface TailAnchor {
  lineId: LineId;
  grapheme: number; // 行内 grapheme 索引，0 表示行首
}
```

如果 `from === null`，表示替换整个 volatile tail。

### 5.2 Envelope

```ts
type StreamKind = "text" | "reasoning" | "tool" | "diff" | "status";
type StreamPriority = 0 | 1 | 2 | 3; // 0 最高

type StreamOperation =
  | { type: "append"; delta: string }
  | { type: "replace-tail"; from: TailAnchor | null; text: string }
  | { type: "finish"; digest?: string }
  | { type: "cancel"; reason: string };

interface StreamEnvelope {
  sessionId: SessionId;
  streamId: StreamId;
  seq: number; // 从 1 开始，连续
  baseRevision: SessionRevision;
  kind: StreamKind;
  priority: StreamPriority;
  op: StreamOperation;
  createdAt: number;
}
```

### 5.3 ApplyResult

```ts
type StreamApplyResult =
  | { status: "applied"; revision: SessionRevision; linesAdded: number }
  | { status: "duplicate"; seq: number }
  | { status: "rejected"; reason: StreamRejectReason };

type StreamRejectReason =
  | "gap"
  | "conflict"
  | "closed"
  | "cancelled"
  | "anchor-outside-volatile-tail"
  | "budget-exceeded"
  | "unknown-stream";
```

### 5.4 Ledger

```ts
interface StreamLedger {
  open(meta: {
    streamId: StreamId;
    kind: StreamKind;
    priority: StreamPriority;
    createdAt: number;
  }): void;

  apply(envelope: StreamEnvelope): StreamApplyResult;

  applyWithSpill(envelope: StreamEnvelope): Promise<StreamApplyResult>;

  /** 显式把全部 spilled lines 读回 ledger；不用于 viewport。 */
  hydrate(streamId: StreamId, lineIds?: readonly LineId[]): Promise<number>;

  /** 定点读取 cold lines，不改变 ledger 状态。 */
  readCold(
    streamId: StreamId,
    lineIds: readonly LineId[]
  ): Promise<readonly StreamLineRecord[]>;

  /** 按逻辑 stable-line 下标读取窗口，自动混合 cold / hot。 */
  readStableRange(
    streamId: StreamId,
    offset: number,
    count: number
  ): Promise<StreamLineWindow>;

  project(streamId: StreamId): StreamProjection;

  compact(streamId: StreamId, policy: StreamRetentionPolicy): CompactionResult;

  stats(streamId?: StreamId): StreamLedgerStats;

  dispose(): void;
}

interface StreamProjection {
  streamId: StreamId;
  status: StreamStatus;
  revision: SessionRevision;
  stableLines: readonly StreamLineRecord[];
  volatileTail: readonly StreamTailLine[];
  tombstones: readonly StreamTombstone[];
  spilledSegments: readonly SpilledSegment[];
}

interface StreamLineWindow {
  streamId: StreamId;
  revision: SessionRevision;
  offset: number;
  totalLines: number;
  lines: readonly StreamLineRecord[];
}

interface SpilledSegment {
  streamId: StreamId;
  firstLineId: LineId;
  lastLineId: LineId;
  count: number;
  bytes: number;
  /** 交错 LineId 的 arithmetic runs，如 start=1, step=4。 */
  lineRuns?: readonly LineIdRun[];
  /** 非规范 ID 无法形成 run 时的显式回退。 */
  lineIds?: readonly LineId[];
}

interface LineIdRun {
  prefix: string;
  start: number;
  step: number;
  count: number;
}

interface StreamLedgerOptions {
  memory?: MemoryLedger;
  spill?: { store: SpillStore; policy?: RetentionPolicy };
  /** applied digest chunk sidecar 基路径；每个 stream 追加安全后缀。 */
  appliedStorePath?: string;
  /** sidecar 内存 LRU chunk 数，默认 64。 */
  appliedCacheChunks?: number;
}

interface FileSpillStoreOptions {
  compactAfterDeletes?: number;
  /** numeric offset/length chunk sidecar 目录。 */
  indexPath?: string;
  /** 内存 LRU chunk 数，默认 64。 */
  indexCacheChunks?: number;
}

interface StreamTailLine {
  id: LineId;
  text: string;
  graphemeLength: number;
}

interface StreamLineRecord {
  id: LineId;
  text: string;
  stableAtRevision: SessionRevision;
  digest: string;
}
```

`createStreamWindow({ ledger, streamId })` 把 `readStableRange()` 适配成只读
`StreamSource`。它只持有当前窗口，滚动使用 generation 防止旧异步请求覆盖新窗口，
并用 revision-aware LRU（默认 8 个窗口）避免返回同一位置时重复 cold read。
`refresh()` 重新读取上一次的 offset / count，并强制绕过缓存。

`createStreamWindowController()` 在 source 上维护 height / offset / page 操作。
预取窗口按 viewport page 对齐，默认上下各一页；缓存可截取覆盖范围，因此在同一页
内滚动不会再次触发 cold read。注入 `FrameClock` 后，`requestScrollTo / By` 使用
同一 coalesceKey，一帧只提交最新 offset；调用方仍负责把 controller 接到键盘 /
鼠标 / scrollbar。

`createStreamWindowInput()` 提供方向键、PageUp / PageDown、Home / End 和滚轮适配；
它调用 controller 的 requestScroll*，因此会自动获得 FrameClock 合并语义。
`createScrollBarForStreamWindow()` 把同一 controller 接到现有 ScrollBar 几何模型。
`<StreamWindow>` 组合 controller、`<StreamText>`、键盘 / 滚轮和可选 ScrollBar，
height 变化会重新加载并夹取窗口；在 `createTuiApp` 内会自动使用 runtime 共享的
FrameClock。

### 5.5 Apply 规则

1. `seq === lastSeq + 1`：应用。
2. `seq <= lastSeq`：
   - digest 相同：`duplicate`；
   - digest 不同：`conflict`。
3. `seq > lastSeq + 1`：`gap`，不应用、不猜测缺失内容。
4. `finish` / `cancel` 后所有后续 op：`closed` / `cancelled`。
5. `replace-tail` anchor 不在 volatile tail：拒绝，不能覆盖 stable line。
6. stream 不存在时返回 `unknown-stream`；由 session layer 决定是否自动 open。
7. 应用前必须向 MemoryLedger 申请 stable + volatile 两份预算。

### 5.6 Compaction 规则

- stable line 已离开 retention window：可压缩 / spill；
- volatile tail 永远不进 cold storage；
- tombstone 至少保留到 replay 的 checkpoint；
- compaction 不得改变 `LineId`、stable line 顺序或 digest；
- 若 spill 后恢复失败，必须报 `cold-read-error`，不能静默丢行。

## 6. MemoryLedger

### 6.1 类型

```ts
type MemoryClass = "ephemeral" | "hot" | "warm" | "cold" | "pinned";
type MemoryPriority = 0 | 1 | 2 | 3;

interface MemoryRequest {
  owner: string;
  class: MemoryClass;
  bytes: number;
  priority: MemoryPriority;
  spillable?: boolean;
  reconstructible?: boolean;
}

type MemoryDecision =
  | { status: "granted"; reservation: MemoryReservation }
  | { status: "spill-required"; candidates: readonly string[] }
  | { status: "rejected"; reason: "budget" | "pinned-limit" };

interface MemoryReservation {
  id: string;
  bytes: number;
  class: MemoryClass;
  resize(nextBytes: number): MemoryDecision;
  release(): void;
}

interface MemoryLedger {
  reserve(request: MemoryRequest): MemoryDecision;
  pressure(): MemoryPressure;
  stats(): MemoryStats;
  setPolicy(policy: MemoryPolicy): void;
}
```

### 6.2 Admission 顺序

```text
ephemeral → hot → warm → cold → pinned
```

预算不足时：

1. 释放 superseded / aborted frame；
2. 释放未展示的 ANSI bytes；
3. 压缩 warm；
4. spill cold；
5. 降低 telemetry / snapshot 密度；
6. 最后发出 `MemoryPressure`，由应用决定降级。

### 6.3 默认 retention

```ts
interface StreamRetentionPolicy {
  hotLines: number;
  hotBytes: number;
  warmBytes: number;
  coldBytes: number;
  tombstoneBytes: number;
  compression?: "none" | "gzip" | "zstd";
  spill?: "none" | "file" | "sqlite";
}
```

默认值应由真实 PTY profile 决定，不在设计阶段拍死。

### 6.4 不变量

- 每次长期 allocation 前必须有 reservation。
- `pinned` 超限时宁可拒绝新 pinned，也不能悄悄转成 hot。
- spill 必须可验证恢复。
- frame bytes 不计入 transcript retention。
- 内存平台期以 retained bytes 验收，不以 GC 时刻验收。

## 7. SessionLog 与 Replay

### 7.1 Record

```ts
interface SessionRecordBase {
  id: string;
  seq: number;
  at: number;
  source: "local" | "remote" | "plugin" | "agent" | "clock";
  cause?: string;
}

type SessionRecord =
  | (SessionRecordBase & { type: "input"; event: ButuiEvent; route: InputRoute })
  | (SessionRecordBase & { type: "resize"; columns: number; rows: number })
  | (SessionRecordBase & { type: "stream"; envelope: StreamEnvelope })
  | (SessionRecordBase & { type: "capability"; action: CapabilityAudit })
  | (SessionRecordBase & { type: "clock"; tick: number; time: number })
  | (SessionRecordBase & { type: "snapshot"; snapshot: SessionSnapshot });
```

### 7.2 Snapshot

```ts
interface SessionSnapshot {
  sessionId: SessionId;
  revision: SessionRevision;
  appState: unknown;
  streamHeads: Readonly<Record<string, number>>;
  focusId?: number;
  viewport?: { columns: number; rows: number };
}
```

### 7.3 Replay

```ts
type ReplayMode = "replay" | "fast" | "headless";

interface ReplayCursor {
  revision(): SessionRevision;
  step(): SessionRecord | undefined;
  runUntil(revision: SessionRevision): void;
  compare(actual: ProjectionSnapshot, expected: ProjectionSnapshot): ReplayDiff;
}
```

规则：

1. reducer 必须纯；时间 / 随机 / I/O 通过 record 注入。
2. replay 不写真实 stdout；使用 MemorySink 或 headless sink。
3. terminal capability 差异不能影响 semantic comparison。
4. replay 遇到未知 record type 必须失败，不能跳过。
5. snapshot 只是加速点，不是真相；删除 snapshot 后仍必须能重放。

## 8. CapabilityBroker

### 8.1 Grant

```ts
interface CapabilityGrant {
  id: CapabilityId;
  pluginId: string;
  slots: ReadonlySet<string>;
  events: ReadonlySet<string>;
  commands: ReadonlySet<string>;
  output: {
    kinds: ReadonlySet<LeaseKind>;
    maxBytesPerSecond?: number;
  };
  memory: {
    maxBytes: number;
    maxFrames?: number;
  };
  expiresAt?: number;
}

interface CapabilityBroker {
  grant(grant: CapabilityGrant): CapabilityToken;
  check(token: CapabilityToken, request: CapabilityRequest): boolean;
  revoke(id: CapabilityId, reason: string): void;
  audit(): readonly CapabilityAudit[];
}
```

### 8.2 规则

1. token 是 runtime 内部 opaque 对象，不能由 plugin 伪造。
2. revoke 后下一次 check 立即失败。
3. event capability 以 semantic region 为粒度，不以“订阅全部事件”为默认。
4. output capability 只能申请子集，arbiter 再做最终仲裁。
5. capability 不是安全沙箱；跨进程安全仍由 OS sandbox / provider 负责。
6. 越权必须审计，不能只返回 false。

### 8.3 Worker capability proxy

```ts
interface WorkerCapabilityBinding {
  capability: PluginCapability;
  handler: (...args: any[]) => unknown | Promise<unknown>;
  authorize?: (
    request: WorkerCapabilityRequest
  ) => boolean | { allowed: boolean; reason?: string } | Promise<...>;
}

createPathPrefixAuthorizer({ roots, cwd, pathArg });
createRateLimitAuthorizer({ limit, windowMs, now });

serveWorkerCapabilities(target, {
  pluginId,
  broker,
  bindings,
  onDenied,
}): () => void;
```

规则：

1. Worker 只能调用 `bindings` 中显式注册的 own method。
2. 每个 binding 必须绑定一个 capability；每次调用都实时调用 `broker.has()`。
3. revoke / TTL 过期后，下一次调用立即失败，不能依赖重连或重新加载。
4. 名称级检查通过后，逐调用 `authorize` 必须再通过；handler 才能执行。
5. `authorize` 返回 false、返回 `allowed: false` 或抛错都必须 fail closed。
6. 拒绝时先产生带 reason / detail 的审计事件，再向 Worker 返回明确错误。
7. path-prefix 是词法约束，不解析 symlink；真实文件访问仍需 realpath / fd / OS sandbox。
8. rate-limit 以 plugin + method 为 key；窗口恢复后允许新调用。
9. endpoint 抽象可接 `Worker` / `MessagePort`；它不提供 OS 级隔离。

### 8.4 Worker supervisor

```ts
createWorkerSupervisor({
  create,
  setup,
  terminate,
  timeoutMs,
  maxRestarts,
  restartDelayMs,
  onEvent,
}): WorkerSupervisor;
```

规则：

1. 每次创建 target 都递增 `generation`；`setup` 必须随 generation 重新执行。
2. 只有当前 generation 的 RPC failure 能触发重启，旧 target 的迟到事件必须忽略。
3. 自动重启受 `maxRestarts` 限制；预算耗尽进入 `failed`，不能无限 crash loop。
4. 重启期间的新调用等待 `ready()`；已经失败的调用不自动重放。
5. 手动 `restart()` 重置自动重启预算，可用于人工恢复。
6. `dispose()` 必须取消等待中的重启、执行 cleanup、终止当前 target 并拒绝后续调用。

### 8.5 Process transport

```ts
createNdjsonRpcEndpoint({ input, output, maxFrameBytes, close });
attachProcessRpc(subprocess);
createProcessStdioEndpoint();
serveProcessRpc(handlers);
```

规则：

1. 使用 UTF-8 NDJSON；一行一个完整 `WorkerRpcMessage`，禁止控制帧跨行。
2. stdout / stdin 是协议通道，不能混入 `console.log` 等非协议输出；诊断走 stderr。
3. 单帧超过 `maxFrameBytes` 必须 fail closed，不能无限增长缓冲区。
4. 子进程 stdout 关闭或读写失败必须触发 endpoint error，由 supervisor 决定重启。
5. JSON 只承载普通 JSON 值；不要发送 `undefined`、BigInt、函数或循环引用。
6. 进程 transport 提供崩溃隔离，不等于 OS 权限沙箱或资源限额。

### 8.6 RPC handshake

```ts
withRpcHandshake(target, {
  protocol,
  version,
  capabilities,
  requiredCapabilities,
  timeoutMs,
}): RpcHandshakeEndpoint;
```

规则：

1. 双方各发送一次 `hello`；`protocol + version` 必须精确匹配。
2. `requiredCapabilities` 必须被对端声明，缺失时 fail-fast。
3. 握手完成前的业务消息必须排队；不能先按旧协议执行再补校验。
4. 超时、协议错配、重复且变化的 hello 都进入 failed 状态。
5. `ready` 暴露对端 capability；`peer` 只应在 ready 后作为路由依据。
6. handshake 是协议兼容检查，不替代 capability authorization。

### 8.7 Process resource limits

```ts
spawnProcessRpc({
  cmd,
  resources: {
    timeoutMs,
    maxOutputBytes,
    maxFrameBytes,
    maxBytesPerSecond,
    killSignal,
    cgroup,
  },
});
```

规则：

1. `timeoutMs`、`maxOutputBytes`、`killSignal` 映射到 Bun.spawn，由 Bun 执行。
2. `maxFrameBytes`、stdout 总字节和 bytes/s token bucket 由 host transport 强制执行。
3. 输出超限必须 fail closed 并终止子进程，不能只停止解析后继续堆积。
4. `cgroup` 只表示加入已有 cgroup；memory.max / cpu.max / pids.max 由外部配置。
5. stderr 是诊断通道，不进入 RPC NDJSON，也不能替代日志限流。
6. 资源限制提供故障隔离，不构成 OS 权限 sandbox。

### 8.8 Cgroup v2 lifecycle

```ts
createCgroupManager({ root, prefix, fs, releaseRetries, retryDelayMs });
manager.detect();
manager.create(name, { memoryMaxBytes, cpuMax, pidsMax });
manager.recover();
manager.releaseAll();
```

规则：

1. 只支持 cgroup v2；root 不可写时必须明确 unsupported，不能静默降级为假限制。
2. create 在独立目录写 memory.max / cpu.max / pids.max，任一失败都回滚目录。
3. release 先写 cgroup.kill=1，再删除；EBUSY 按预算重试。
4. recover 只处理 manager 自有 prefix 的 stale 目录，不能扫描并删除任意 cgroup。
5. lease 传入 spawnProcessRpc 后，子进程退出时自动 release。
6. cgroup 委派、权限和挂载由部署层负责；本模块不创建宿主机级权限模型。

### 8.9 Audit log

```ts
createMemoryAuditLog({ now, maxEvents });
createFileAuditLog({ path, now, maxMemoryEvents });
readAuditLog(path, { type, pluginId, since, until, limit });
```

规则：

1. 事件必须有单调 `seq`、`at`、`type`；可选 `pluginId` 与结构化 detail。
2. 文件审计使用 NDJSON append；内存只保留有界尾部，完整历史从文件读取。
3. query 支持 type / pluginId / 时间范围和 limit，不改变日志内容。
4. capability grant / revoke / deny、process start / error / exit / kill、
   cgroup create / release / recover 必须能形成同一条审计链。
5. 审计失败不能反向破坏受审计操作；需要显式 flush 检查持久化结果。
6. audit 是观测与追责输入，不是 capability 判定或 OS sandbox 本身。

## 9. 不变量清单

| ID | 不变量 | 验收方式 |
|---|---|---|
| I1 | FrameId / revision 单调，不重用 | fake clock 单测 |
| I2 | 鼠标只命中 `presentedFrame` | backpressure + hit test |
| I3 | accepted 后不撤销，未 accepted 可 supersede | transport 单测 |
| I4 | control bytes 不因 latest-wins 丢失 | lease 单测 |
| I5 | raw lease 释放后强制 full damage | PTY 集成 |
| I6 | stream gap / conflict 不静默 | ledger 单测 |
| I7 | replace-tail 不越界覆盖 stable line | ledger 单测 |
| I8 | 长期 allocation 先过 MemoryLedger | stress + instrumentation |
| I9 | replay 不依赖 wall-clock / 真实 stdout | fast replay CI |
| I10 | capability revoke 立即生效 | plugin 集成 |
| I11 | 任意退出路径恢复 terminal mode | signal / crash 测试 |
| I12 | 未知 stdout writer 只做 best-effort，不虚假承诺 | 文档 + 集成边界 |

## 10. 原型测试矩阵

### 10.1 FrameClock

- input 到达时 decorative lane 超预算；
- reveal 积压 10k 字符时 critical 仍当帧提交；
- fake clock 两次运行产生同一 frame 序列；
- 降级后连续满足预算，升档不振荡。

### 10.2 TerminalArbiter

- `write(false)` 后 frame 被 supersede，drain 后只写最新；
- append 打断 frame，下一帧 full；
- raw 等待 accepted，release 后恢复鼠标 / cursor；
- plugin lease 超时强制释放；
- control 与 frame barrier 顺序正确。

### 10.3 Presented-frame hit test

- 写阻塞期间布局变化，用户点击仍命中旧视觉 frame；
- target node 删除后按 semantic id 回退；
- drag capture 不因 frame 更新而漂移；
- resize 后旧 frame 坐标失效并标记 stale。

### 10.4 StreamLedger

- append 正常；
- seq 缺口拒绝；
- 重复 seq 幂等；
- 同 seq 不同 digest 冲突；
- replace-tail anchor 在 stable line 时拒绝；
- finish 后 append 拒绝；
- cancel 后 replay 仍有 tombstone；
- compact / spill 后 LineId 和文本一致。

### 10.5 MemoryLedger

- 100k / 1M 行输入时 retained bytes 有上界；
- spill 后恢复 transcript 一致；
- pinned 超限拒绝而不是偷换类别；
- frame supersede 后 bytes 立即可回收。

### 10.6 Replay

- 同一日志在 live / fast / headless 得到相同 semantic snapshot；
- ANSI 可不同，semantic diff 必须相同；
- 删除中间 snapshot 后仍可重放；
- 未知 record type 明确失败。

## 11. v0.1 adapter 计划

### 11.1 FrameClock adapter

```ts
// 旧行为：RenderScheduler
createTuiApp({ render: { mode: "frame", fps: 120 } });

// 新实现内部映射为：
clock.request({
  lane: "critical",
  reason: "mutation",
  sessionRevision,
  coalesceKey: "runtime-frame",
  work: runFrame,
});
```

v0.1 测试必须保持通过；如果 frame 数变化，先调整 adapter，不改应用行为。

### 11.2 Terminal adapter

`TerminalSession.write()` 保留，内部可包成 `TerminalArbiter.write()`。
`onDrain()` 继续存在，但新的 `FrameBarrier` 是更完整的同步语义。

### 11.3 Stream adapter

```ts
interface LegacyStreamAdapterOptions {
  width: number;
  streamId: StreamId;
  kind?: StreamKind;
}

function createEnvelopeStream(
  ledger: StreamLedger,
  options: LegacyStreamAdapterOptions
): StreamSource;
```

旧的 `push(delta)` 映射为 `append`，`flush()` 映射为 `finish`。
新增 `replaceTail(anchor, text)` / `cancel(reason)` 作为可选扩展。

### 11.4 Memory adapter

没有配置 budget 时使用 `unbounded` policy 保持 v0.1 行为，但内部仍记录 stats。
启用 budget 后才允许 spill / compact / reject。

## 12. 需要真实数据后再决定的问题

1. 120fps 下 FrameClock 的 p95 是否真的能稳定低于 8.33ms？
2. `critical` 和 `reveal` 是否应该合并为一个 transaction，还是保持双 lane？
3. MemoryLedger 默认按 bytes 还是按 lines 主导？
4. spill 后端选文件、SQLite 还是 Bun 自带的哪种能力？
5. Plugin UI capability 是否应使用 structured clone 可传递 token？
6. `presented` 是否可以通过终端同步输出 ACK 做得更准确？
7. StreamLedger 的 compaction 是否值得在 v0.2 第一版实现？
8. Web / SSH adapter 是否共享 TerminalLease，还是只共享 SessionLog？

这些问题的答案必须来自 bugent 真实 PTY、慢终端、100k 行会话和插件崩溃测试，
不能靠设计期直觉提前定死。
