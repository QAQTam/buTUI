# v0.2 P0：FrameClock 详细设计

> 前置：[V0.2_FRONTIER.md](./V0.2_FRONTIER.md)  
> 契约：[V0.2_CONTRACTS.md](./V0.2_CONTRACTS.md) §2 / §3  
> 目标：统一 runtime、animation、smooth reveal 的时间源，并让 120fps 成为可测量、
> 可降级、可回放的预算，而不是多个 `setInterval` 的偶然结果。
>
> P0-A 原型：[packages/core/src/frame-clock.ts](./packages/core/src/frame-clock.ts)
> / [tests/frame-clock.test.ts](./tests/frame-clock.test.ts)
>
> 当前验证：相关定向套件 48 pass；`TERM=xterm-256color` 下全量 627 pass / 0 fail。

## 0. 设计结论

1. 所有**周期性视觉工作**必须订阅同一个 FrameClock。
2. 输入处理不走 FrameClock 队列；它同步完成，然后请求一个 critical frame。
3. FrameClock 只负责决定“什么时候执行哪类工作”，不直接持有 layout / renderer。
4. 工作函数必须是同步的；异步结果作为新 revision 重新请求。
5. `presented` 是 transport 的结果，不是 layout 完成的时间。
6. v0.1 `RenderScheduler` 保留为 adapter，先保持行为，再逐项迁移。

## 1. 目标与非目标

### 目标

- 2000 chunk/s 下合帧，但输入不被 reveal / shimmer 拖慢。
- 120fps 目标下 p95 compute budget 可观测。
- 空闲后的第一次视觉变化走微任务，不凭空增加一帧延迟。
- stdout backpressure 时停止无效动画工作，恢复后只提交最新 revision。
- fake clock 下完全确定性，可重放 frame 序列。
- v0.1 `render: { mode, fps }` 行为兼容。

### 非目标

- 不保证每个终端物理刷新 120fps。
- 不把异步 I/O、网络、磁盘写入塞进 frame budget。
- 不为不同组件建立独立 timer。
- 不用 wall-clock 决定 reducer 结果。

## 2. 运行时模型

```text
terminal input ──同步──→ session reducer / focus / state
                           │ revision
                           ▼
                     FrameClock.request(critical)
                           │
                    ┌──────┴──────┐
                    │  next tick  │
                    └──────┬──────┘
                           ▼
            flush → layout → damage → encode
                           │
                           ▼
                 TerminalArbiter / lease
                           │
                    accepted / blocked
                           │
                    drained / presented
                           │
                           ▼
                    lastPresentedFrame
```

FrameClock 不知道 FrameRecord 的完整生命周期，但接收 transport 的 outcome：

```ts
type ClockFrameOutcome =
  | { status: "presented"; frameId: FrameId; at: number }
  | { status: "blocked"; frameId: FrameId; at: number }
  | { status: "drained"; frameId: FrameId; at: number }
  | { status: "superseded"; frameId: FrameId; at: number }
  | { status: "aborted"; frameId: FrameId; at: number; reason: string };

interface FrameClock {
  request(request: FrameRequest): FrameRequestHandle;
  cancel(handle: FrameRequestHandle): void;
  settle(outcome: ClockFrameOutcome): void;
  advanceTo(time: number): void;
  stats(): FrameClockStats;
}
```

`settle` 由 transport 在状态变化时调用。没有 transport 的 headless / 兼容模式可以在
paint 返回后立即调用 `presented`。

## 3. Lane 与 deadline

### 3.1 Lane 定义

| Lane | 是否同步 | 默认 cadence | 可降级 | 典型工作 |
|---|---:|---:|---:|---|
| `input` | 是 | 无 | 否 | 输入解码后的状态提交 |
| `critical` | 否 | frame budget | 否 | layout、focus、最终文本 |
| `reveal` | 否 | frame budget | 合并 / 加速 | smooth stream |
| `decorative` | 否 | frame budget | 暂停 | shimmer、惯性 |
| `maintenance` | 否 | 低频 | 暂停 | telemetry、压缩、debug |

`input` 不作为普通 FrameRequest 执行。它的作用是让 runtime 可以记录输入耗时，
并在必要时立刻把 `critical` 提到微任务。

### 3.2 Deadline

```ts
interface FrameRequest {
  lane: Exclude<FrameLane, "input">;
  reason: string;
  sessionRevision: SessionRevision;
  coalesceKey?: string;
  deadline?: number;
  work: (tick: FrameTick) => FrameWorkResult;
}
```

Deadline 计算：

```text
critical:
  first request after idle → queueMicrotask
  otherwise                → max(now, nextFrameAt)

reveal:
  deadline = max(now, nextFrameAt)

decorative:
  deadline = max(now, nextFrameAt)

maintenance:
  deadline = max(now, nextFrameAt, lastMaintenanceAt + maintenanceInterval)
```

`nextFrameAt = lastDispatchAt + interval`，不是 `lastPresentedAt + interval`。
这样慢终端阻塞恢复后不会补发一串过期帧；blocked 状态本身会阻止 dispatch。

## 4. 调度算法

### 4.1 request

```ts
function request(req: FrameRequest): FrameRequestHandle {
  if (disposed) return cancelledHandle;

  const previous = queue.get(req.coalesceKey);
  if (!previous || req.sessionRevision >= previous.sessionRevision) {
    queue.set(req.coalesceKey, req);
  }

  scheduleWake();
  return handleFor(req);
}
```

规则：

- 同 key 只保留最新 revision；
- 不同 key 即使同 lane 也不互相覆盖；
- request 本身不执行 layout；
- `cancel` 只取消未执行请求；
- 已进入 `work()` 的请求不可取消，只能通过 revision supersede。

### 4.2 scheduleWake

```ts
function scheduleWake(): void {
  if (disposed || blocked || timerScheduled || microtaskScheduled) return;

  const next = earliestDeadline(queue);
  if (next === undefined) return;

  const now = this.now();
  if (now >= next) {
    microtaskScheduled = true;
    queueMicrotask(runDue);
    return;
  }

  timerScheduled = true;
  setTimeout(runDue, next - now);
}
```

- 同一时刻最多一个 microtask 或 timer；
- 空闲后第一次 request 若已经过 frame deadline，走 microtask；
- timer 不重置：新请求只更新 queue，不推迟已排定的帧；
- fake clock 下 `advanceTo()` 直接调用 `runDue`。

### 4.3 runDue

```ts
function runDue(): void {
  clearWakeFlags();
  if (disposed || blocked) return;

  const now = this.now();
  const due = collectDueRequests(queue, now);
  if (due.length === 0) {
    scheduleWake();
    return;
  }

  const tick = createTick(now);
  let usedMs = 0;

  for (const req of orderByLane(due)) {
    if (!canRun(req, usedMs, tick.budgetMs)) continue;

    const started = this.now();
    const result = req.work(tick);
    usedMs += Math.max(0, this.now() - started);

    consume(req, result, tick);
  }

  lastDispatchAt = this.now();
  observeDispatch(usedMs, due, tick);

  scheduleWake();
}
```

关键点：

- `work` 同步执行；
- critical 永远允许运行，即使已经超预算；
- reveal / decorative / maintenance 受剩余 budget 限制；
- 被 budget 跳过的请求保留在 queue，下一帧再试；
- 同一个 coalesceKey 每 tick 最多执行一次；
- work 返回 dirty revision 时，用新 revision 重新入队。

### 4.4 Lane 顺序

```text
critical > reveal > decorative > maintenance
```

输入不在此顺序中，因为它已经在 FrameClock 之前同步完成。

若 critical 超预算：

1. 当前 tick 不再执行 reveal / decorative / maintenance；
2. 记录 `budgetExceededBy = "critical"`；
3. 下一 tick 仍从 critical 开始；
4. AdaptiveQuality 根据连续超预算决定降级。

## 5. Frame budget

### 5.1 目标值

120fps：

```text
frame interval: 8.333ms
compute budget: 6.000ms
transport slack: 2.333ms
```

这里的 compute 包含：

- Solid flush；
- projection / layout；
- damage 计算；
- ANSI encode；
- 同步 afterDraw hooks。

不包含：

- `stdout.write()` 之后的 OS / 终端耗时；
- 异步图片解码；
- 网络和工具 I/O；
- 插件进程 IPC。

### 5.2 分片

| Lane | min slice | max slice | 超预算行为 |
|---|---:|---:|---|
| critical | 无上限 | 无上限 | 执行并报警 |
| reveal | 0.5ms | 3ms | 下一帧继续 |
| decorative | 0.25ms | 1ms | 跳过 |
| maintenance | 0.25ms | 1ms | 延后 |

分片只约束“是否开始一个工作”，不强行中断已经开始的同步函数。若单个
layout 本身超过 budget，AdaptiveQuality 必须能看到 phase 级统计。

### 5.3 FrameTick

```ts
interface FrameTick {
  clockTime: number;
  frameId: FrameId;
  quality: QualityLevel;
  budgetMs: number;
  sessionRevision: SessionRevision;
  phase: "dispatch";
}
```

## 6. Reveal 控制器

smooth reveal 不自己起 timer，使用 reveal lane。建议算法：

```text
lag = targetColumns - shownColumns
if lag <= 0: idle

base = speedColumnsPerSecond * dt
catchUp = lag / catchUpSeconds * dt
advance = min(lag, base + catchUp, maxColumnsPerFrame)
```

使用定点 / 浮点 accumulator 保存不足 1 列的推进，避免 120fps 下因 `floor()`
产生漂移。

质量策略：

| Quality | reveal 行为 |
|---|---|
| `full` | 基础速度 + 180ms catch-up |
| `balanced` | 基础速度 + 120ms catch-up |
| `responsive` | 基础速度 + 80ms catch-up |
| `minimal` | 直接显示 target |

当 lag 很大：

- 不增加无上限的 `maxColumnsPerFrame`；
- 缩短 catch-up 窗口；
- 仍然逐帧推进，避免“喷洒”；
- 最终 revision 改变时，旧 target 直接丢弃，不为追赶旧内容工作。

## 7. Backpressure

### 7.1 blocked

Transport 收到 `write() === false`：

```ts
clock.settle({ status: "blocked", frameId, at: now });
```

FrameClock 进入 blocked：

- 停止 scheduleWake；
- 允许 input / session reducer 继续；
- 只保留最新 revision 的 queue；
- reveal 不再推进，但 target 继续增长；
- decorative 不累计债务；
- critical 保持 dirty。

### 7.2 drain

```ts
clock.settle({ status: "drained", frameId, at: now });
```

恢复：

1. 清除 blocked；
2. 丢弃被 supersede 的 queued frames；
3. 将 critical 请求放到 microtask；
4. 下一帧强制 full damage；
5. 根据 blocked duration 更新 AdaptiveQuality；
6. reveal 按当前 target 重新计算 lag，不回放旧帧。

## 8. AdaptiveQuality

### 8.1 输入信号

```ts
interface QualitySignals {
  computeP95Ms: number;
  presentedIntervalP95Ms: number;
  blockedRatio: number;
  eventLoopDelayP95Ms: number;
  inputLatencyP95Ms: number;
  skippedFrameCount: number;
}
```

采样窗口建议：

- compute：最近 120 个 dispatch；
- presented interval：最近 120 个 presented；
- blocked ratio：最近 1 秒；
- event-loop delay：最近 1 秒；
- input latency：最近 100 次 input。

### 8.2 降级

满足任一条件，连续 30 个样本后降一级：

```text
computeP95Ms > budget * 1.20
presentedIntervalP95Ms > targetInterval * 1.25
blockedRatio > 0.10
inputLatencyP95Ms > targetInterval
skippedFrameCount >= 30
```

### 8.3 升档

满足全部条件，连续 2 秒后升一级：

```text
computeP95Ms < budget * 0.70
presentedIntervalP95Ms < targetInterval * 1.10
blockedRatio == 0
eventLoopDelayP95Ms < 2ms
inputLatencyP95Ms < targetInterval
```

### 8.4 固定策略

- `BUTUI_REDUCED_MOTION=1`：禁用 reveal / decorative，最低降到 `responsive`。
- `TERM=dumb` / `NO_COLOR` 不影响 FrameClock quality，只影响 rendering capability。
- 应用可以设置 quality ceiling，例如 `maxQuality: "balanced"`。
- replay 默认锁定记录中的 quality，除非显式请求 deterministic recalculation。

## 9. Observability

每次 dispatch 产生一条低开销事件，只在开发模式或采样时记录：

```ts
interface FrameDispatchSample {
  frameId: FrameId;
  requestedAt: number;
  dispatchedAt: number;
  computeMs: number;
  budgetMs: number;
  quality: QualityLevel;
  ran: readonly FrameLane[];
  skipped: readonly FrameLane[];
  dirtyRevision?: SessionRevision;
  supersededCount: number;
}
```

每次 presentation：

```ts
interface FramePresentationSample {
  frameId: FrameId;
  dispatchedAt: number;
  acceptedAt: number;
  presentedAt: number;
  blockedMs: number;
  bytes: number;
  damageLines: number;
}
```

统计要求：

- ring buffer，默认最多 240 条；
- `stats()` 不分配大对象；
- production 默认只保留聚合值；
- telemetry 写入不能进入 critical budget。

## 10. v0.1 兼容 adapter

### 10.1 RenderScheduler

| v0.1 | v0.2 adapter |
|---|---|
| `mode: "microtask"` | critical + immediate microtask |
| `mode: "frame", fps` | critical + frame cadence |
| `markPainted()` | `settle(presented)` |
| `request()` | `FrameClock.request()` |
| `cancel()` | 取消 handle |

要求：

- `tests/render-scheduler.test.ts` 不修改也能通过；
- `mode: "microtask"` 的第一次 request 仍走 microtask；
- frame 模式下同一预算内不重置尾帧；
- 首帧和 idle 后首帧不额外等待。

### 10.2 AnimationScheduler

`useAnimationFrame()` 的公开签名不变：

```ts
const time = useAnimationFrame();
```

内部从独立 `setInterval` 改为：

```ts
clock.request({
  lane: enabled() ? "decorative" : "maintenance",
  reason: "animation",
  sessionRevision,
  coalesceKey: "animation",
  work: tick => listeners(tick.clockTime),
});
```

若组件需要 `full` quality 下的独立帧率，可创建命名 lane 或 group；第一版不开放
任意 lane 扩展。

### 10.3 SmoothStream

`createSmoothStream()` 的 `scheduler` 注入暂时保留，用于测试。默认路径改为
FrameClock reveal subscription。所有 smooth stream 共用一个 reveal lane，不按
fps 创建多个 scheduler。

## 11. 原型切片

### P0-A：FrameClock 核心（原型已完成）

- request / coalesce / cancel；
- critical + reveal 两 lane；
- fake clock；
- budget 统计；
- v0.1 RenderScheduler adapter 已接入，原行为测试保持不变；
- AnimationScheduler 已改为 FrameClock 驱动，并支持注入共享 clock；
- SmoothStream 已接入共享 reveal lane，优先使用 runtime clock。

### P0-B：Transport settle（原型已完成）

- accepted / blocked / drained / presented；
- blocked 时停止 FrameClock dispatch；
- drain 后只画最新 revision；
- stop / dispose 会解除 blocked，避免重启后永久停帧；
- full damage on resume 仍由 TerminalArbiter 阶段补齐。

### P0-C：Animation / Smooth 迁移（原型已完成）

- AnimationScheduler 接入 decorative；
- SmoothStream 接入 reveal，并优先使用 runtime 注入的 clock；
- runtime 向 AppScope 提供同一个 FrameClock / AnimationScheduler；
- reduced-motion 行为不变；
- 120fps demo 与 bench 对比仍待真实 PTY 数据。

### P0-D：AdaptiveQuality（原型已完成）

- `AdaptiveQuality` 采样 compute ratio、presented interval、blocked ratio、
  input latency 和 skipped frames；
- 连续坏样本降一级，连续好样本满足 hold time 才升一级；
- FrameClock 可选接入，`render.adaptiveQuality` 默认关闭以保持 v0.1 行为；
- 输入优先的真实 PTY 验证仍待 benchmark；
- event-loop delay 暂为预留信号，当前由 compute / blocked / skipped 驱动。

## 12. 验收测试

1. 空闲后第一次 request 不等待完整 frame。
2. 同一 coalesceKey 在一个 frame 内只执行一次。
3. critical 超预算时 reveal / decorative 被跳过。
4. critical 不因 reveal 积压而延迟超过 1 个 microtask。
5. fake clock 相同请求序列得到相同 frame 序列。
6. `write(false)` 后 clock 停止 dispatch。
7. drain 后只执行最新 critical revision。
8. 10k 字符 reveal 不一次刷完，且能在目标时间内追平。
9. reduced-motion 下 reveal / decorative 不运行。
10. v0.1 RenderScheduler 测试全部保持通过。
11. 输入 latency 在 decorative 饱和时不超过 1 帧。
12. telemetry ring buffer 不随运行时间增长。

## 13. 待验证决策

- `lastDispatchAt` 是否应改为“上次开始 work 的时刻”还是“上次完成 work 的时刻”？
- critical 超预算时是否允许在同一 tick 执行 0ms 的 maintenance？
- reveal catch-up 是否应感知终端 backpressure 的预计恢复时间？
- quality 降级是否应由每个 app 独立决定，还是允许进程级共享？
- `performance.now()` 在 Bun 与 fake clock 下的精度是否足以做 1ms 分片？

这些问题先由 P0 原型和真实 PTY profile 回答，不提前把策略写死。
