# buTUI v0.2 Isolation Report

> 日期：2026-09-24
> 环境：Linux x64 / Bun 1.4.3
> 脚本：`scripts/plugin-isolation-bench.ts`

## 1. 目标

比较 Worker 与 process 两种隔离 transport 的基础成本，给插件后端默认策略提供数据：

- 首次 RPC 前的启动时间；
- 顺序 RPC 的 p50 / p95 / p99；
- 固定 concurrency 下的 RPC 吞吐；
- 父进程 heap 增量；
- process 的最大 RSS。

本轮只测本地 echo workload，不包含真实业务 CPU、payload 大小或终端 I/O。

## 2. 命令

```bash
bun --conditions=browser run scripts/plugin-isolation-bench.ts \
  --calls=5000 \
  --warmup=200 \
  --concurrency=32
```

## 3. 结果

| mode | startup | p50 | p95 | p99 | concurrent rpc/s | parent heap Δ | maxRSS |
|---|---:|---:|---:|---:|---:|---:|---:|
| worker | 2.80 ms | 0.017 ms | 0.032 ms | 0.048 ms | 260,801 | 0.33 MB | - |
| process | 142.63 ms | 0.036 ms | 0.060 ms | 0.077 ms | 161,557 | 1.46 MB | 95.9 MB |

说明：

- `startup` 从创建隔离体到第一次 `echo` 返回。
- 顺序 RPC 和并发吞吐各执行 5,000 次。
- Worker 的 heap / RSS 不在父进程 `process.memoryUsage()` 中，因此不报告。
- process 的 `maxRSS` 来自 `Bun.Subprocess.resourceUsage()`。

## 4. 结论

1. **热路径**：Worker 启动和 RPC 延迟明显更低，适合可信的 CPU 解析、高亮、计算型插件。
2. **强隔离**：process 启动约多两个数量级，单次 p95 仍低于 0.1ms；额外成本主要在启动和 RSS，
   适合不可信、需要 cgroup / crash 隔离的插件。
3. **渲染插件**：当前 SlotRenderer 是同步契约，不应跨 RPC 渲染；UI 贡献仍应在应用进程内，
   隔离后端用于服务、解析、索引和后台计算。
4. **默认策略**：可信插件默认 in-process；可配置 Worker；不可信或资源密集型插件使用 process + cgroup。

## 5. 边界

- 数据受本机调度、Bun canary 版本和进程冷启动影响。
- echo payload 很小；大 payload 会放大 JSON / structured clone 和内存成本。
- 尚未测 supervisor 重启、capability deny 和 audit sink 开销。
- 真实 bugent workload 仍未接入，本报告不能替代端到端 profile。
