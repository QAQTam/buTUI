# v0.2 参考实现与 Bun 沙箱可行性

> 分支：`design/v0.2.0`  
> 参考源码：
>
> - Ink `02ae1e5`：`/home/qaqtamsy/项目/reference/ink`
> - OpenTUI `4b1474e`：`/home/qaqtamsy/项目/reference/opentui`
> - Bun sandbox branch `675ec8c768`：`/home/qaqtamsy/项目/bun`
>   (`feat/spawn-sandbox-hook`)

## 0. 结论

### TS + Bun 可以直接做

- Renderable / node tree / reconciler 适配。
- 事件、focus、keymap、鼠标、paste、resize、OSC。
- 增量布局编排、滚动视口、damage 描述。
- stream reveal、frame clock、backpressure。
- worker 中的 Tree-sitter / 解析 / 语法高亮。
- `bun:ffi` / `bun-ffi-structs` 边界和 WASM fallback。

### 适合 WASM 或可选 Rust

- ANSI / grapheme / width / wrap 内核。
- 大规模 text buffer / rope / edit buffer。
- cell buffer diff 和 ANSI 输出编码。
- 复杂布局测量（先看真实 profile）。
- 图片缩放 / 编解码等纯计算内核。

### 必须依赖系统或 native

- OS 沙箱（Landlock / seccomp / Seatbelt / AppContainer）。
- 严格剪贴板、部分终端图形协议和原生窗口集成。
- 跨进程权限隔离、资源限制、进程树控制。

### 不建议

- 因为 OpenTUI 用 Zig，就把 buTUI runtime 整体重写成 Rust / Zig。
- 用 Worker 冒充安全沙箱。
- 用 `node:vm` 作为不可信代码边界。
- 在没有真实 profile 前引入 native core。

## 1. OpenTUI 实际架构

OpenTUI 的源码结构是：

```text
@opentui/core       TypeScript API + FFI 绑定
@opentui/native     Zig 实现
@opentui/solid      Solid renderer / catalogue
@opentui/react      React renderer
@opentui/keymap     独立 keymap
@opentui/ssh        远程终端层
@opentui/web / three 额外渲染后端
```

关键事实：

- `packages/core/src/zig.ts` 通过 Bun FFI / Node experimental FFI 调 native。
- `OptimizedBuffer` 是 native 内存上的 cell buffer，布局和绘制大量通过 FFI。
- Yoga 也是 native Zig 实现，不是 WASM。
- `NativeSpanFeed` 用 native ring / shared memory 和 Node Writable 回调实现背压。
- native renderer 有 stdout / memory / buffered / feed backend，可选 render thread。
- TS 层仍然负责 Renderable tree、事件分发、组件、键盘、终端协议和生命周期。

值得借鉴：

- `NativeSpanFeed` 的“native 生成帧、JS 异步消费、回调完成才释放 chunk”背压模型。
- `targetFps` + `maxFps` 双阈值。
- Renderable tree / catalogue / RenderContext 的职责划分。
- frame stats 和可观测性。
- 输出 backend 可替换，而不是把 stdout 写死进 renderer。

不照搬：

- native cell buffer 成为所有状态唯一来源。
- 每个组件操作都穿 FFI。
- 为音视频、3D、SSH 等场景扩张核心。
- Linux 上 render thread 目前仍被关闭，说明“native = 自动更快”不成立。

## 2. Ink 实际架构

Ink 是纯 TS/JS 路线：

```text
React reconciler
  → DOM-like node tree
  → Yoga WASM layout
  → render-node-to-output
  → log-update / stdout
```

关键事实：

- `yoga-layout` 是 WASM；布局 API 不要求 native runtime。
- `render-node-to-output` 负责把节点树转成 ANSI 字符串。
- `log-update` 用行数擦除和重写实现增量刷新。
- `write-synchronized.ts` 使用 BSU/ESU（DEC mode 2026）减少闪烁。
- `maxFps` 默认 30，`incrementalRendering` 可切换更新策略。
- `<Static>` 把永久内容留在终端 scrollback，动态区域单独刷新。
- focus ID / focus manager / kitty keyboard 都有成熟语义。

值得借鉴：

- `maxFps`、`incrementalRendering`、`onRender(metrics)` 的公共 API。
- `waitUntilRenderFlush()`：背压和测试都需要一个“这一帧真的写出去了”的承诺。
- `<Static>`：聊天历史不需要每帧重排，天然适合 scrollback。
- 非 TTY 模式：只保留最终帧，不做终端控制序列。
- focus ID 语义：相同 focus ID 可视为一个 Tab stop。

不照搬：

- React reconciler / Fiber 生命周期。
- 用整段 ANSI 字符串做唯一中间表示。
- 默认 30fps 和每帧重写行策略。
- Yoga WASM 的内存所有权模型和 `freeRecursive` 手工释放要求。

## 3. 能力矩阵

| 能力 | TS + Bun | WASM | Rust native | 结论 |
|---|---:|---:|---:|---|
| Solid reconciler | 已实现 | 不需要 | 不需要 | 保持 TS |
| Renderable / focus / keymap | 已实现 | 不需要 | 不需要 | 保持 TS |
| 增量 layout | 已实现 | 可选 | 不建议 | 先 profile |
| ANSI / width / wrap | 已实现 | 高收益候选 | 备选 | WASM 优先 |
| cell diff / ANSI encode | 已实现 | 高收益候选 | 备选 | 用 damage 做边界 |
| text buffer / rope | 基础实现 | 高收益候选 | 可选 | bugent 大数据后再开 |
| image codec / resize | Bun API + TS | 不必要 | 部分需要 | 保持 Bun API |
| Tree-sitter | worker + WASM | 已适合 | 不必要 | 保持 worker |
| 进程沙箱 | 只能编排 | 不适用 | 必要 | Bun sandbox provider |
| in-process 权限 | 无 | 无 | 需要 runtime 支持 | 不建议第一版做 |
| 跨进程隔离 | Bun.spawn | 不适用 | OS 能力 | 第一版走子进程 |

## 4. Bun 沙箱现状

本地 Bun 分支已有提交：

```text
675ec8c768 feat(spawn): add native sandbox provider hook
branch: feat/spawn-sandbox-hook
```

新增能力：

```ts
Bun.spawn(["command"], {
  sandbox: {
    library: "/path/to/provider.so",
    config: JSON.stringify({ read: [...], write: [...], network: false }),
  },
});
```

ABI：

```c
void* bun_spawn_sandbox_prepare(const char* config, size_t len, int* errno_out);
int bun_spawn_sandbox_apply(void* state);
void bun_spawn_sandbox_destroy(void* state);
```

生命周期：

1. parent 中 `prepare`，加载 provider、解析配置、准备 child 状态。
2. child 在 uid/gid、fd cleanup 之后、`execve` 之前调用 `apply`。
3. parent 中 `destroy`，释放 provider state。

边界：

- provider 是 trusted native code，运行在 Bun 进程内。
- `apply` 在 Linux 的 vfork child 中执行，必须 async-signal-safe。
- 目前 Windows 明确不支持。
- 目前只有 hook，没有内置 Landlock / seccomp / Seatbelt provider。
- Worker 只是线程和堆隔离，不是文件 / 网络权限沙箱。

因此：

- **不需要为了 hook 再 fork 一个 Bun。**
- bugent 现有 `feat/spawn-sandbox-hook` 分支可以作为沙箱能力的上游基线。
- 真正需要新增的是 provider 和策略层，不是再次改 `Bun.spawn` 的入口。

## 5. 推荐的 bugent 沙箱架构

第一版不要试图在同一个 JS realm 里限制不可信代码。用进程边界：

```text
bugent host
  → SandboxManager
    → Bun.spawn({
        cmd: [bun, plugin-entry],
        sandbox: { library: sandbox-provider, config },
        stdio: ["pipe", "pipe", "pipe"],
      })
    → NDJSON / framed IPC
```

策略：

- 插件代码只在 child Bun 进程执行。
- child 只允许读取 runtime、插件目录和显式 workspace。
- 写入只允许指定 workspace / temp。
- 默认禁止网络、进程创建、任意动态库加载。
- host 通过 capability token / IPC 请求高风险操作。
- Worker 用于 CPU 隔离和崩溃隔离，不承担权限安全。

平台实现顺序：

1. Linux：Landlock（文件）+ seccomp（syscall / network）+ `no_new_privs`。
2. macOS：Seatbelt / sandbox profile。
3. Windows：AppContainer / restricted token / Job Object，暂不进入第一版。

## 6. 什么时候才需要 fork Bun

只有在下面任一条件成立时才建议长期维护 fork：

1. 需要在 **Worker 内部** 强制文件 / 网络 / 子进程权限，而不是只限制 child。
2. 需要 Bun runtime 自己暴露 `--allow-read` / `--allow-write` / `--allow-net`
   之类的 JS 级 permission model。
3. 需要内置 provider ABI 的跨平台扩展，例如 Windows AppContainer。
4. 需要把 sandbox policy 与 Bun 的 module loader、FFI、`Bun.spawn` 统一到同一个
   capability context。
5. 上游不接受 sandbox ABI 演进，但 bugent 又必须依赖它。

否则继续维护一个 provider package 更便宜：

```text
@bugent/sandbox-provider-linux
@bugent/sandbox-provider-darwin
```

它们只实现 C ABI，不修改 Bun runtime。

## 7. v0.2 实施建议

### 保留纯 TS 的部分

- `@butui/runtime`、Solid renderer、组件、keymap、focus、terminal 协议。
- damage / display list 先由 TS 实现。
- smooth reveal、FrameClock、AdaptiveQuality 继续 TS。

### 新增可选加速边界

```text
@butui/accel-protocol
  → TS backend
  → WASM backend
  → native backend（Rust / Zig 均可）
```

先做 WASM spike：

- ANSI parse / SGR state。
- grapheme width / wrap。
- line / span diff。

只有通过 v0.2 的收益门槛，才增加 Rust native backend。

### Bun / 沙箱并行线

- 基于 `feat/spawn-sandbox-hook` 做 Linux provider 原型。
- provider 可以使用 Rust `cdylib`，因为它实现的是 C ABI，不要求改 Bun。
- bugent 第一版优先做“子进程插件 + IPC + capability 审批”。
- 是否 fork Bun 等 provider 原型跑通后再决定。

## 8. 只考虑 TUI：需要 fork Bun 吗

结论：**不需要。当前 Bun 已覆盖 TUI runtime 所需的宿主能力。**

已有且够用：

| 需求 | Bun 能力 | 是否需要 fork |
|---|---|---:|
| ANSI / Unicode width / wrap / slice | `Bun.stringWidth` / `wrapAnsi` / `sliceAnsi` | 否 |
| Markdown | `Bun.markdown` | 否 |
| 终端 raw mode / resize / 输入 | Node streams + `Bun.Terminal` | 否 |
| stdout 背压 | Node `Writable` / `write()` / `drain` | 否 |
| 高性能输出 sink | `Bun.stdout.writer()` / `FileSink` | 否 |
| native 内核 | `bun:ffi` + `toArrayBuffer` / `ptr` | 否 |
| native → JS 回调 | `JSCallback({ threadsafe: true })` | 否 |
| worker / CPU 隔离 | Web Worker / `node:worker_threads` | 否 |
| 共享内存 | `SharedArrayBuffer` + `Atomics` | 否 |
| PTY / 子终端 | `Bun.Terminal` | 否 |
| 子进程 | `Bun.spawn` | 否 |
| 帧调度 | `performance.now` / `setTimeout` / `setImmediate` | 否 |
| 信号 / 退出恢复 | `process.on` / signal-exit 模式 | 否 |

TUI 核心中适合自己实现的层：

- damage / display list。
- frame clock 和 adaptive quality。
- ANSI 输入解码、Kitty keyboard、mouse / paste / focus。
- synchronized output（BSU/ESU）。
- cell diff 和 renderer backend。
- terminal capability 探测。
- smooth reveal / stream scheduling。
- layout / scroll / virtual viewport。

只有这些情况才值得新增 Bun API：

| 潜在 API | 用途 | 是否 TUI 必需 |
|---|---|---:|
| Worker 资源上限（heap / stack / CPU） | 防止插件或解析 worker 拖垮进程 | 否，插件能力 |
| Worker 文件 / 网络 capability scope | 真正的 in-process 权限隔离 | 否，沙箱能力 |
| 内置跨平台 sandbox provider | 插件 / 工具进程沙箱 | 否，沙箱能力 |
| 更稳定的 thread-safe FFI callback 生命周期 API | native renderer 高级场景 | 否，现有 API 可做 |
| native span feed / ring buffer | 极致零拷贝帧输出 | 否，可用户态实现 |

因此：

- **纯 TUI 不 fork Bun。**
- OpenTUI 式 native core 可以通过 `bun:ffi` + Rust/Zig 库实现，不需要改 Bun。
- 如果引入不可信插件，才把 fork 议题移到 `bugent` 的沙箱分支。
- 如果未来要做“Bun 原生权限模型”，它应服务所有 Bun 应用，而不是只为 TUI 加特殊 API。

## 9. 最终建议

**OpenTUI：借鉴接口、Renderable 模型、NativeSpanFeed 和 backend 分层。**  
**Ink：借鉴调度、Static、focus 语义、maxFps 和 flush 语义。**  
**buTUI：保持 TS+Bun 宿主，先做 damage / WASM 加速边界。**  
**bugent 沙箱：复用 Bun 已有 spawn hook，先做 provider 和子进程隔离；不要为沙箱立刻 fork 整套 Bun。**
