# v0.2 Retention 压测报告

> 基准：`scripts/stream-retention-bench.ts`
> 配置：batch=500，memory=64KB，retain=32KB
> 日期：2026-09-24

## 结论

当前实现已经让文本预算、hot lines 和 cold-store I/O 都进入可控状态：

| Lines | elapsed | heapUsed | RSS | ledgerUsed | inMemoryLines | spilledLines | cold file |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 10k | 139ms | 8.00MB | 91.1MB | 40KB | 4,140 | 5,860 | 0.81MB |
| 100k | 1.12s | 9.04MB | 115.3MB | 55KB | 4,776 | 95,224 | 13.63MB |
| 1M | 10.50s | 17.52MB | 156.6MB | 60KB | 4,478 | 995,522 | 145.56MB |

`100k -> 1M` 时：

- heap 只增加约 `8.6MB`，与 numeric index 的 `9 bytes/line` 基本一致；
- `ledgerUsed` 保持在 64KB 预算内；
- `inMemoryLines` 稳定在约 4.5k；
- cold file 按文本量线性增长；
- 1M 行吞吐约 `95k lines/s`，相比逐条 I/O 版本的约 `4.3k lines/s`
  提升约 `22x`。

## 本轮实现

1. `FileSpillStore` 对规范数字 LineId 使用 `Map<chunk, Uint32Array>`：
   - 每 1024 个 LineId 一个 chunk；
   - 稀疏编号不会分配巨型数组；
   - `line-01` 等非规范编号走普通 Map，避免与 `line-1` 冲突；
   - 超过 4GiB 的文件 offset 自动回退普通 Map。
2. `SpillStore` 增加可选 `writeMany()` / `readMany()`：
   - spill 写入一次系统调用；
   - 写后校验只打开一次文件；
   - 返回数组与输入 LineId 下标一一对应。
3. `rebuildIndex()` 改为 64KB 流式扫描：
   - 不再把整个 cold file 读进内存；
   - `StringDecoder` 可跨 chunk 正确处理 UTF-8。
4. `compact()` 改为流式重写：
   - 通过 live-offset 判断保留最新记录；
   - 不在 compact 时构造全量 record 数组；
   - 完成后重新建立索引。

## Reopen 实测

`1M --reopen`：

| 指标 | 值 |
|---|---:|
| cold file | 145.56MB |
| records | 995,522 |
| reopen elapsed | 1.84s |
| heap before | 17.53MB |
| heap after | 29.17MB |
| index 增长 | 约 11.6MB |
| 随机读取最后一条 spilled line | 成功 |

流式扫描避免了 145MB 文件的内存峰值；reopen 后仍保留约 9 bytes/line
的直接寻址索引。

## Viewport cold read

`StreamLedger.readCold()` 从 spilled segment 中读取指定 LineId，不修改 ledger：

| Lines | requested | elapsed | heap delta | ledger 状态 |
|---:|---:|---:|---:|---|
| 100k | 1,000 | 4.7ms | 0 | 不变 |
| 1M | 1,000 | 5.0ms | 0 | 不变 |

这使 viewport / replay 可以只读取可见 cold lines，不需要先 full hydrate。
多 stream 交错分配全局 LineId 时，spilled segment 优先使用 arithmetic runs，
不会再压缩成错误范围，也不会回退成逐行字符串数组。

`readStableRange(offset, count)` 进一步隐藏 LineId 映射：它按逻辑 stable-line
下标自动混合 spilled 与 hot lines。1M 行测试中读取跨越冷热边界的 1,000 行：

| offset | requested | returned | cold / hot | elapsed | heap delta |
|---:|---:|---:|---:|---:|---:|
| 995,022 | 1,000 | 1,000 | 500 / 500 | 2.2ms | 0 |

返回窗口不改变 ledger，也不需要调用方知道 segment 或 run 的布局。
`createStreamWindow()` 将同一窗口适配为只读 `StreamSource`，可直接挂到
`<StreamText>`；快速滚动时只有最新 generation 的异步结果可以提交。默认 LRU
缓存 8 个 revision-aware 窗口，`refresh()` 强制绕过缓存。
`createStreamWindowController()` 进一步维护 height / offset / page，并按 viewport
页对齐预取；同一页内滚动可直接命中缓存。
`createStreamWindowInput()` 与 `createScrollBarForStreamWindow()` 已把键盘、
滚轮和 scrollbar 映射到同一 controller。

## 多 stream metadata

基准：`scripts/stream-multiplex-bench.ts --lines=100000 --streams=4`

| spilled lines | segments | lineRuns | explicit lineIds |
|---:|---:|---:|---:|
| 92,720 | 4 | 4 | 0 |

四路 stream 每路形成：

```text
{ prefix: "line-", start: 1|2|3|4, step: 4, count: N }
```

该基准按单行 round-robin 写入，是最容易暴露交错元数据问题的路径；其
`32.6s` 主要来自 100k 次单行 `applyWithSpill`，不是 metadata 扫描成本。

## Hydrate 实测

full hydrate 改为每 1,024 条调用一次 `readMany()`；每个 batch 只打开一次文件。

| Lines | restored | elapsed | heap before | heap after | RSS after |
|---:|---:|---:|---:|---:|---:|
| 100k | 95,224 | 297ms | 9.06MB | 23.46MB | 148.8MB |
| 1M | 995,522 | 3.04s | 17.53MB | 142.40MB | 575.9MB |

这里的内存增长来自“恢复后的 transcript 本身”，不是 cold-read 临时缓冲。
`hydrate()` 的语义仍是显式 full materialization；viewport 应使用 `readCold()`，
只有确实需要把内容重新放回 ledger 时才调用 hydrate。

## 仍未解决

1. numeric index 仍是约 `9 bytes/line` 的线性 metadata。1M 行约 9MB，
   占 cold text 的约 6%；如果目标是 10M 行以上的严格常数内存，需要改成
   sparse checkpoint + file scan，或把索引放到磁盘。
2. 多 stream 的规则 stride 已压缩为 `lineRuns`；极不规则的交错仍可能回退到
   显式 `lineIds`，需要真实 agent 事件分布验证是否值得继续做 RLE。
3. `StreamLedger.applied` 仍按 batch seq 保留 digest；当前 benchmark 中
   只有 2,000 项，不是主要增长项，但长期会话需要 checkpoint/GC 策略。
4. `createStreamWindowController()` 已提供窗口缓存、竞态防护、height / offset /
   page 操作和按页预取；键盘 / 滚轮 / ScrollBar adapter 已实现，下一步是把它们
   组合进实际 transcript component 和 runtime 事件入口。
5. RSS 包含 Bun runtime、文件页缓存和未强制回收页，不能直接等同于 JS heap；
   后续需要加入 GC 前后双采样。

## 复现

```bash
bun --conditions=browser run scripts/stream-retention-bench.ts \
  --lines=1000000 --batch=500

bun --conditions=browser run scripts/stream-retention-bench.ts \
  --lines=1000000 --batch=500 --reopen

bun --conditions=browser run scripts/stream-retention-bench.ts \
  --lines=1000000 --batch=500 --hydrate

bun --conditions=browser run scripts/stream-retention-bench.ts \
  --lines=1000000 --batch=500 --cold-read=1000

bun --conditions=browser run scripts/stream-retention-bench.ts \
  --lines=1000000 --batch=500 --window=1000

bun --conditions=browser run scripts/stream-multiplex-bench.ts \
  --lines=100000 --streams=4
```
