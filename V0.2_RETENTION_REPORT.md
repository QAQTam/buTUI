# v0.2 Retention 压测报告

> 基准：`scripts/stream-retention-bench.ts`  
> 配置：batch=100，memory=64KB，retain=32KB  
> 日期：2026-09-24

## 结论

当前实现已经让**文本预算和 in-memory stable line 进入平台期**：

| Lines | elapsed | heapUsed | RSS | ledgerUsed | inMemoryLines | spilledLines | cold file |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 10k | 147ms | 7.8MB | 141MB | 49KB | 4,440 | 5,560 | 775KB |
| 100k | 1.67s | 28.0MB | 259MB | 63.8KB | 4,176 | 95,824 | 13.75MB |

文本内容已经从内存移到 cold storage；`ledgerUsed` 没有随 100k 行增长，`inMemoryLines`
稳定在约 4.2k。

## 尚未解决

`heapUsed` 仍从约 7.8MB 增长到 28MB，主要剩余线性项是：

1. `FileSpillStore` 当前为每条 cold record 保留一个 `Map<string, offset>` 项；
2. `StreamLedger.applied` 为每个 batch seq 保留 digest，属于低频但仍有增长；
3. spilled segment 的 `firstLineId / lastLineId` 已 compact，不再是主要问题。

因此当前不能宣称“完全内存平台期”，只能宣称“文本预算与 hot lines 平台期”。

## 下一轮优化

1. 把 `FileSpillStore` 改成 sequential line number + numeric offset array，
   去掉每行字符串 key 的 Map 开销；
2. 对 1M 行重复测试，目标：
   - `inMemoryLines` 平台期；
   - `ledgerUsed` 平台期；
   - `FileSpillStore` metadata 远小于文本体积；
   - heap 增长接近常数或只保留低频 checkpoint。
3. 如果 numeric offset array 仍不够，改成 sparse checkpoint + file scan。
