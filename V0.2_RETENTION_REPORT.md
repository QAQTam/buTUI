# v0.2 Retention 压测报告

> 基准：`scripts/stream-retention-bench.ts`
> 配置：batch=500，memory=64KB，retain=32KB
> 日期：2026-09-24

## 结论

当前实现已经让文本预算、hot lines 和 cold-store I/O 都进入可控状态：

| Lines | elapsed | heapUsed | RSS | ledgerUsed | inMemoryLines | spilledLines | cold file |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 10k | 139ms | 8.00MB | 91.1MB | 40KB | 4,140 | 5,860 | 0.81MB |
| 100k | 1.12s | 9.04MB | 115.3MB | 55KB | 4,776 | 95,224 | 13.63MB |
| 1M | 10.50s | 17.52MB | 156.6MB | 60KB | 4,478 | 995,522 | 145.56MB |

`100k -> 1M` 时：

- heap 只增加约 `8.6MB`，与 numeric index 的 `9 bytes/line` 基本一致；
- `ledgerUsed` 保持在 64KB 预算内；
- `inMemoryLines` 稳定在约 4.5k；
- cold file 按文本量线性增长；
- 1M 行吞吐约 `95k lines/s`，相比逐条 I/O 版本的约 `4.3k lines/s`
  提升约 `22x`。

## 本轮实现

1. `FileSpillStore` 对规范数字 LineId 使用 `Map<chunk, Uint32Array>`：
   - 每 1024 个 LineId 一个 chunk；
   - 稀疏编号不会分配巨型数组；
   - `line-01` 等非规范编号走普通 Map，避免与 `line-1` 冲突；
   - 超过 4GiB 的文件 offset 自动回退普通 Map。
2. `SpillStore` 增加可选 `writeMany()` / `readMany()`：
   - spill 写入一次系统调用；
   - 写后校验只打开一次文件；
   - 返回数组与输入 LineId 下标一一对应。
3. `rebuildIndex()` 改为 64KB 流式扫描：
   - 不再把整个 cold file 读进内存；
   - `StringDecoder` 可跨 chunk 正确处理 UTF-8。
4. `compact()` 改为流式重写：
   - 通过 live-offset 判断保留最新记录；
   - 不在 compact 时构造全量 record 数组；
   - 完成后重新建立索引。

## Reopen 实测

`1M --reopen`：

| 指标 | 值 |
|---|---:|
| cold file | 145.56MB |
| records | 995,522 |
| reopen elapsed | 1.84s |
| heap before | 17.53MB |
| heap after | 29.17MB |
| index 增长 | 约 11.6MB |
| 随机读取最后一条 spilled line | 成功 |

流式扫描避免了 145MB 文件的内存峰值；reopen 后仍保留约 9 bytes/line
的直接寻址索引。

## Hydrate 实测

full hydrate 改为每 1,024 条调用一次 `readMany()`；每个 batch 只打开一次文件。

| Lines | restored | elapsed | heap before | heap after | RSS after |
|---:|---:|---:|---:|---:|---:|
| 100k | 95,224 | 297ms | 9.06MB | 23.46MB | 148.8MB |
| 1M | 995,522 | 3.04s | 17.53MB | 142.40MB | 575.9MB |

这里的内存增长来自“恢复后的 transcript 本身”，不是 cold-read 临时缓冲。
`hydrate()` 的语义仍是显式 full materialization；viewport 渲染不应调用它，
后续需要增加不改变 ledger 状态的 partial cold-read API。

## 仍未解决

1. numeric index 仍是约 `9 bytes/line` 的线性 metadata。1M 行约 9MB，
   占 cold text 的约 6%；如果目标是 10M 行以上的严格常数内存，需要改成
   sparse checkpoint + file scan，或把索引放到磁盘。
2. `StreamLedger.applied` 仍按 batch seq 保留 digest；当前 benchmark 中
   只有 2,000 项，不是主要增长项，但长期会话需要 checkpoint/GC 策略。
3. RSS 包含 Bun runtime、文件页缓存和未强制回收页，不能直接等同于 JS heap；
   后续需要加入 GC 前后双采样。

## 复现

```bash
bun --conditions=browser run scripts/stream-retention-bench.ts \
  --lines=1000000 --batch=500

bun --conditions=browser run scripts/stream-retention-bench.ts \
  --lines=1000000 --batch=500 --reopen

bun --conditions=browser run scripts/stream-retention-bench.ts \
  --lines=1000000 --batch=500 --hydrate
```
